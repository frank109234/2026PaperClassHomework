# 当前自适应评论选择架构：方法论汇报说明稿

> 建议汇报时长：结合公式完整讲解约 12--15 分钟  
> 对应正文：`overleaf_methodology_adaptive_selector_architecture_v2.tex`  
> 本稿只讲模型架构，不展开训练流程和实验参数。

## 一、开场：我们要解决什么问题

各位老师好，下面我介绍本文的方法部分。

本文研究的是结合新闻正文和用户评论的假新闻检测。对于一条新闻，我们不仅输入新闻正文，还输入这条新闻下面的多条用户评论，最终判断新闻是真实还是虚假。

这里的关键问题是：评论并不是越多越好。有些评论能够提供事实补充、质疑、辟谣或佐证，但也有些评论只是闲聊、情绪表达或者与新闻无关。如果把所有评论不加区分地交给分类器，无关内容可能会掩盖真正有价值的证据。

因此，本文模型不是直接聚合全部评论，而是先回答一个问题：对于当前这条新闻，哪些评论值得作为真假判断的证据？模型先形成一个数量可变的评论子集，再使用这个子集完成新闻真假分类。

## 二、概览：模型由哪些部分组成

整个模型可以概括为五个连续模块。

第一，共享 BERT 编码器。新闻和评论都使用同一套 Chinese BERT-wwm-ext 编码，使二者位于同一个语义空间。

第二，多尺度评论选择表示。选择器不只读取 BERT 的全局表示，还在 BERT 的 token 序列上使用卷积核为 2、3、4 的多尺度 CNN，提取不同长度的局部语义。

第三，新闻级自适应评论选择。模型一方面计算每条评论与新闻的关系分数，另一方面根据新闻和整组评论预测这条新闻自己的选择分界。评论分数超过当前新闻的分界时，才自然进入证据子集。

第四，评论证据聚合。模型在入选评论内部重新分配权重，把数量不固定的评论压缩成一个固定维度的评论证据向量。

第五，真假分类。分类器显式比较新闻表示和评论证据表示，最终输出真实新闻和虚假新闻的概率。

这里最重要的变化是：旧方法让所有新闻共享同一个固定阈值，而当前方法为每条新闻预测独立的选择分界。

## 三、共享 BERT 编码

首先，新闻正文和每条评论都输入同一套 BERT。

对于第 n 条新闻，BERT 会输出一组 token 级上下文表示，同时输出 `[CLS]` 对应的全局表示。每条评论也得到相同形式的输出。

对应公式为：

$$
\mathbf{H}_{n}^{x}=E_{\mathrm{BERT}}(x_n),
\qquad
\mathbf{g}_{n}^{x}=\mathbf{H}_{n}^{x}[0],
$$

$$
\mathbf{H}_{ni}^{c}=E_{\mathrm{BERT}}(c_{ni}),
\qquad
\mathbf{g}_{ni}^{c}=\mathbf{H}_{ni}^{c}[0].
$$

这里，$\mathbf{H}$ 表示整段文本全部 token 的上下文表示，$\mathbf{g}$ 表示 `[CLS]` 全局表示；上标 $x$ 表示新闻，上标 $c$ 表示评论。

这份 BERT 输出会进入两个功能不同的分支。

- 评论选择分支读取完整的 token 序列，因为它需要识别评论中的局部短语、否定表达、质疑和事实片段。
- 真假分类分支使用 `[CLS]` 全局表示，因为分类器需要稳定地概括新闻和评论证据的整体语义。

两个分支虽然使用表示的方式不同，但底层 BERT 是共享的，因此新闻和评论始终处于一致的语义空间。

## 四、多尺度 CNN、尺度注意力和门控残差

### 4.1 为什么需要多尺度 CNN

BERT `[CLS]` 擅长表达整段文本的大致含义，但评论中的关键线索可能只是一个很短的局部片段，例如“并非如此”“已经辟谣”或者“没有这回事”。因此，选择器不仅使用 `[CLS]`，还继续读取 BERT 输出的完整上下文序列 $\mathbf{H}_{ni}^{c}$。

需要强调的是，CNN 的输入不是原始词向量，而是已经经过 BERT 上下文编码的 token 表示。因此，同一个词在不同语境中的表示可以不同，CNN 提取的是带有上下文信息的局部模式。

模型设置卷积核集合 $\mathcal{K}=\{2,3,4\}$：

- 卷积核为 2，观察连续 2 个 token；
- 卷积核为 3，观察连续 3 个 token；
- 卷积核为 4，观察连续 4 个 token。

例如，一条评论经过分词后得到“官方、已经、明确、辟谣、了”。卷积核为 2 时会依次观察“官方--已经”“已经--明确”“明确--辟谣”等局部窗口；卷积核为 4 时则可以观察“官方--已经--明确--辟谣”这种相对完整的短语。这里的 token 由 BERT 分词器决定，不一定严格对应单个汉字或完整词语。

对于第 $n$ 条新闻的第 $i$ 条评论，在尺度 $k$ 下，卷积和激活过程写为：

$$
\mathbf{A}_{nik}^{c}
=\mathrm{GELU}
\left(
\mathrm{Conv1D}_{k}(\mathbf{H}_{ni}^{c})
\right).
$$

这里，$\mathrm{Conv1D}_{k}$ 使用长度为 $k$ 的窗口沿评论滑动，$\mathbf{A}_{nik}^{c}$ 保存该尺度在所有有效位置上的局部响应。不同卷积通道会学习识别不同模式，例如否定、质疑、佐证或事实描述。

同一个通道会在评论的多个位置产生响应，因此模型随后执行带掩码的最大池化：

$$
\mathbf{u}_{nik}^{c}
=\mathrm{MaskedMaxPool}
\left(\mathbf{A}_{nik}^{c}\right)
\in\mathbb{R}^{d_c}.
$$

$d_c$ 是每个卷积分支的通道数，当前为 128。最大池化会在每个通道中保留整条评论响应最强的位置。例如某个通道的响应为 0.12、0.30、0.91 和 0.24，池化后保留 0.91。这样，关键短语无论出现在评论开头、中间还是结尾，都可以被提取出来。

这里使用 `MaskedMaxPool`，是因为不同评论长度不同，短评论需要 padding。掩码会排除 padding token 以及落在 padding 上的无效卷积窗口，避免补齐位置被误认为局部特征。

经过池化后，三个尺度分别得到 $\mathbf{u}_{ni2}^{c}$、$\mathbf{u}_{ni3}^{c}$ 和 $\mathbf{u}_{ni4}^{c}$。这些向量是 128 维，而 BERT 隐藏表示是 $d$ 维，因此每个尺度还要通过独立投影层映射到 BERT 空间：

$$
\mathbf{v}_{nik}^{c}
=\mathrm{LN}
\left[
\mathrm{GELU}
(\mathbf{W}_{k}\mathbf{u}_{nik}^{c}+\mathbf{b}_{k})
\right]
\in\mathbb{R}^{d}.
$$

这里，$\mathbf{W}_{k}$ 和 $\mathbf{b}_{k}$ 是尺度 $k$ 自己的投影参数，LN 表示层归一化。三个尺度分别投影而不是提前混合，可以保留 2-token、3-token 和 4-token 特征各自的尺度身份，并为后面的尺度注意力提供维度一致的输入。

因此，多尺度局部特征模块完成的是“分别提取”，而不是“决定哪个尺度最重要”：

```text
BERT 上下文 token 表示
→ 2/3/4 三种 CNN 分别扫描
→ GELU 激活
→ 掩码最大池化保留最强响应
→ 各尺度独立投影到 BERT 空间
```

下一步的尺度注意力才负责根据当前文本，为这三种尺度动态分配权重。新闻正文也使用相同的多尺度 CNN 和尺度投影过程得到局部特征。

### 4.2 尺度注意力在选择什么

三种尺度并不是简单拼接，也不是使用固定比例。模型会根据当前文本动态判断 2、3、4 三种尺度各自应该占多大权重。

首先，模型对三种尺度特征求平均，得到当前文本的局部特征摘要：

$$
\overline{\mathbf{v}}_{ni}^{c}
=\frac{1}{|\mathcal{K}|}
\sum_{k\in\mathcal{K}}\mathbf{v}_{nik}^{c}.
$$

这里的平均只负责概括“三种尺度整体提取到了什么”，并不直接判断哪个尺度更重要。模型将这个局部摘要与 BERT `[CLS]` 全局表示拼接，再通过可学习的投影生成查询向量：

$$
\mathbf{q}_{ni}^{c}
=\tanh\left[
\mathbf{W}_{q}
(\mathbf{g}_{ni}^{c}\Vert\overline{\mathbf{v}}_{ni}^{c})
+\mathbf{b}_{q}
\right].
$$

因此，查询向量 $\mathbf{q}_{ni}^{c}$ 同时反映“整段文本主要在说什么”和“三种局部特征整体呈现什么模式”。参数 $\mathbf{W}_{q}$ 与 $\mathbf{b}_{q}$ 会在模型学习过程中逐渐学会：面对不同语义背景，应当重点寻找哪类局部特征。

尺度注意力可以写为：

$$
\alpha_{nik}^{c}
=\mathrm{softmax}_{k\in\mathcal{K}}
\left(
\frac{(\mathbf{q}_{ni}^{c})^{\top}\mathbf{v}_{nik}^{c}}{\sqrt d}
\right),
$$

$$
\mathbf{l}_{ni}^{c}
=\sum_{k\in\mathcal{K}}\alpha_{nik}^{c}\mathbf{v}_{nik}^{c}.
$$

其中，$\mathcal{K}=\{2,3,4\}$，$\alpha_{nik}^{c}$ 是第 $k$ 个尺度的注意力权重，$\mathbf{l}_{ni}^{c}$ 是三个尺度加权后的局部表示。

点积衡量查询向量与某个尺度特征在各语义维度上的匹配程度：方向越一致，点积通常越大，说明该尺度提取的模式越符合当前文本需要。除以 $\sqrt d$ 是为了防止维度较高时点积过大；Softmax 再把三个匹配分数转换为和为 1 的相对权重。最后，三个尺度按这些权重加权求和，而不是只保留其中一个尺度。

例如，一条以短促否定为主的评论可能更依赖 2-token 特征，一条包含较完整事实描述的评论可能更依赖 4-token 特征。尺度权重会随输入文本变化。新闻正文也经过相同的尺度注意力过程。

这里要注意，尺度注意力和评论选择器不是同一个概念。

- 尺度注意力发生在单条文本内部，选择 2、3、4 哪种局部尺度更重要。
- 评论选择器发生在一条新闻的多条评论之间，选择哪些评论更适合作为证据。

### 4.3 门控残差如何融合局部和全局语义

尺度注意力得到的是局部语义，而 BERT `[CLS]` 保存的是全局语义。模型使用门控残差把二者结合起来。

例如，评论“这件事官方已经辟谣了”的 BERT 全局表示能够概括整句话的意思，CNN 局部表示则可能突出“官方辟谣”这个关键短语。但是，“有人说官方辟谣了，但我不相信”同样包含“官方辟谣”。如果直接把全部 CNN 特征加入全局表示，就可能放大脱离上下文的局部线索。因此，模型不是直接相加，而是先判断局部信息应该采用多少。

首先，模型对加权后的局部表示做一次非线性变换，使其更适合与 BERT 全局表示融合：

$$
\widetilde{\mathbf{l}}_{ni}^{c}
=\mathrm{Dropout}
\left[
\mathrm{GELU}
(\mathbf{W}_{l}\mathbf{l}_{ni}^{c}+\mathbf{b}_{l})
\right].
$$

接着，模型把全局表示和变换后的局部表示一起输入门控层：

$$
\mathbf{a}_{ni}^{c}
=\sigma\left[
\mathbf{W}_{g}
(\mathbf{g}_{ni}^{c}\Vert\widetilde{\mathbf{l}}_{ni}^{c})
+\mathbf{b}_{g}
\right].
$$

这对应正式方法论中的公式（12）。可以把它读成：“同时查看整句话的意思和其中的局部线索，决定局部信息的各个部分应该采用多少。”这里的拼接只是给门控层提供判断依据，还没有真正融合两路表示。Sigmoid 函数 $\sigma$ 将每个门控值限制在 0 到 1 之间。

$\mathbf{a}_{ni}^{c}$ 是一个 $d$ 维向量，而不是整条评论共用的单一数值。例如，$\mathbf{a}_{ni}^{c}=[0.1,0.8,0.3,\ldots]$ 表示对应维度的局部信息分别采用 10%、80% 和 30%。所以，公式（12）的作用可以概括为“制定融合方案”。

随后，模型按照这个方案真正完成融合。对应公式（13）的最终表示为：

$$
\mathbf{r}_{ni}^{c}
=\mathrm{LN}
\left(
\mathbf{g}_{ni}^{c}
+\mathbf{a}_{ni}^{c}\odot\widetilde{\mathbf{l}}_{ni}^{c}
\right).
$$

这条公式可以读成：“先按门控比例过滤 CNN 局部表示，再把保留下来的局部信息补充到 BERT 全局表示中。”$\odot$ 表示逐元素乘法。假设某一维满足 $g_j=0.7$、$\widetilde l_j=0.8$、$a_j=0.25$，那么融合结果为

$$
g_j+a_j\widetilde l_j
=0.7+0.25\times0.8
=0.9.
$$

虽然该维局部特征原本为 0.8，但门控只允许其中的 25% 加入。门控值接近 0 时，模型主要保留该维的 BERT 全局信息；接近 1 时，则更多加入对应的局部信息。

这里的“残差”是指 $\mathbf{g}_{ni}^{c}$ 存在一条直接通路，不需要经过局部门控即可进入最终表示。因此，即使某些 CNN 特征不可靠，模型也不会丢失原始的 BERT 全局语义；局部特征主要起补充和修正作用。LN 表示层归一化，用来稳定两路特征相加后的数值尺度。

所以，$\mathbf{g}_{ni}^{c}$ 和 $\widetilde{\mathbf{l}}_{ni}^{c}$ 在两个公式中出现并不是重复融合：在公式（12）中，它们是制定融合方案的判断依据；在公式（13）中，它们才是被实际融合的内容。简单来说，公式（12）负责作决定，公式（13）负责执行这个决定。

需要区分两种“门控”：这里的 $\mathbf{a}_{ni}^{c}$ 是单条文本内部的特征级向量门控，控制局部特征如何进入表示；后面的评论选择门控则是每条评论对应的标量，控制该评论是否进入证据子集。新闻正文也使用同样的门控残差模块得到选择表示。

## 五、新闻和评论的关系打分

经过前面的表示模块后，模型分别得到新闻选择表示和每条评论的选择表示。

接下来，模型对每一个新闻--评论对构造四部分关系：

```text
新闻表示
评论表示
新闻与评论的绝对差
新闻与评论的逐元素乘积
```

前两部分保留新闻和评论自身的信息；绝对差反映二者在各语义维度上的差异；逐元素乘积反映二者在哪些维度上相互匹配。

四路关系函数写为：

$$
\Phi(\mathbf{h}_1,\mathbf{h}_2)
=\mathbf{h}_1\Vert\mathbf{h}_2
\Vert|\mathbf{h}_1-\mathbf{h}_2|
\Vert(\mathbf{h}_1\odot\mathbf{h}_2),
$$

其中，$\Vert$ 表示向量拼接。第 $i$ 条评论的关系分数为：

$$
s_{ni}
=\mathrm{MLP}_{\mathrm{sel}}
\left[
\Phi(\mathbf{r}_{n}^{x},\mathbf{r}_{ni}^{c})
\right].
$$

四部分拼接后输入选择 MLP，得到评论关系分数。这个分数不是脱离新闻语境的通用评论质量分数，而是表示：这条评论对于当前这条新闻有多大的证据价值。

同一条评论放在不同新闻下面，可能得到不同分数，因为它与新闻之间的关系发生了变化。

## 六、新闻级自适应选择分界

这是当前架构与旧固定阈值方法最核心的区别。

旧方法会给所有新闻统一设置一个阈值，例如 0.20。这样隐含了一个假设：不同新闻的评论分数处于同一个稳定标尺上。但实际上，不同新闻的主题、评论数量和评论质量可能差异很大，统一阈值不一定适合所有新闻。

因此，本文为每条新闻预测独立的选择分界。

### 6.1 M 和 M_n 分别表示什么

模型为每条新闻最多预留 M 个候选评论位置，当前架构中 M 等于 32。M 对所有新闻相同。

第 n 条新闻实际拥有的有效评论数记为 M_n。M_n 随新闻变化，并且不会超过 M。不足 32 条的部分只是 padding，不算有效评论。

例如，一条新闻只有 8 条有效评论，那么：

```text
M = 32
M_n = 8
评论数量比例 rho_n = 8 / 32 = 0.25
```

使用这个比例，可以让模型知道当前评论集合的相对规模。

对应公式为：

$$
\rho_n=\frac{M_n}{M}.
$$

### 6.2 分界网络读取哪些信息

分界网络主要读取两类信息。

第一类是“这些评论主要在说什么”。模型把当前新闻全部有效评论的选择表示求平均，得到评论集合摘要。这样，无论一条新闻有多少评论，最终都可以得到一个固定维度的集合表示。

当 $M_n>0$ 时，评论集合摘要为：

$$
\overline{\mathbf{r}}_{n}^{c}
=\frac{1}{M_n}\sum_{i=1}^{M_n}\mathbf{r}_{ni}^{c}.
$$

第二类是“当前有多少条评论”。模型使用刚才介绍的评论数量比例 rho_n。

随后，模型比较新闻选择表示和评论集合摘要，仍然使用新闻自身、评论摘要、绝对差和逐元素乘积四种关系，再拼接评论数量比例。这个关系向量输入分界 MLP，得到当前新闻自己的分界 b_n：

$$
\mathbf{z}_{n}^{\mathrm{thr}}
=\Phi(\mathbf{r}_{n}^{x},\overline{\mathbf{r}}_{n}^{c})
\Vert\rho_n,
$$

$$
b_n=b_0+\mathrm{MLP}_{\mathrm{thr}}
\left(\mathbf{z}_{n}^{\mathrm{thr}}\right).
$$

其中，$b_0$ 是基础偏置，$\mathrm{MLP}_{\mathrm{thr}}$ 根据新闻、评论集合和评论数量产生样本级修正。

b_n 较高时，评论需要获得更高的关系分数才能入选；b_n 较低时，选择条件相对宽松。

因此，即使两条新闻中的某条评论得到相同分数，也可能因为两条新闻的评论集合和评论数量不同，最终产生不同的选择结果。

## 七、从自适应分界得到变长评论子集

对于第 n 条新闻的第 i 条评论，模型比较评论关系分数 s_ni 和新闻分界 b_n。

可以把门控强度理解为：

$$
q_{ni}
=\sigma\left(\frac{s_{ni}-b_n}{T}\right),
$$

其中，$T$ 是温度参数，用于控制 Sigmoid 变化的平滑程度。

如果 s_ni 大于等于 b_n，也就是 q_ni 大于等于 0.5，这条评论自然进入证据集合。否则，它不会自然入选。

因此，自然评论集合写为：

$$
\mathcal{S}_{n}^{0}
=\{i\mid s_{ni}\geq b_n\}
=\{i\mid q_{ni}\geq0.5\}.
$$

这意味着每条新闻的选择结果都可以不同。例如：

```text
新闻 A：自然选出 7 条评论
新闻 B：自然选出 3 条评论
新闻 C：自然选出 1 条评论
```

当前架构还设置了最低证据数 3。如果一条新闻本身有足够评论，但自然选择不足 3 条，模型会从剩余评论中补充关系分数最高的评论，直到达到 3 条。

这个机制不是固定 Top-3。假如新闻 A 自然选出了 7 条，模型仍会保留这 7 条，不会裁剪成 3 条。最低证据约束只处理选择结果过少的情况。

因此，最终得到的是一个具有最低证据保障的变长评论子集。

## 八、评论证据聚合

得到评论子集以后，还需要把多条评论转换成分类器能够使用的固定维度表示。

这里有两个容易混淆的量。

- q_ni 是评论超过新闻分界的门控强度。
- w_ni 是这条评论在最终证据向量中的相对权重。

模型只在入选评论内部对 q_ni 进行归一化，使所有入选评论的 w_ni 之和等于 1。

$$
w_{ni}
=\frac{q_{ni}}
{\sum_{j\in\mathcal{S}_n}q_{nj}},
\qquad i\in\mathcal{S}_n.
$$

例如，三条入选评论的门控强度分别为 0.8、0.6 和 0.2，总和为 1.6，那么对应权重为：

```text
0.8 / 1.6 = 0.500
0.6 / 1.6 = 0.375
0.2 / 1.6 = 0.125
```

随后，模型使用这些权重对入选评论的 BERT `[CLS]` 表示加权求和，得到新闻级评论证据向量。

$$
\mathbf{e}_n
=\sum_{i\in\mathcal{S}_n}
w_{ni}\mathbf{g}_{ni}^{c}.
$$

这里选择分支和分类分支存在明确分工：

- 加入多尺度 CNN 的选择表示负责决定哪些评论进入证据集合；
- 评论的 BERT `[CLS]` 负责向分类器提供入选评论的整体语义内容。

无论最终选中了 3 条、7 条还是更多评论，聚合后都得到一个固定维度的评论证据向量，因此分类器可以统一处理不同大小的评论集合。

## 九、新闻与评论证据联合分类

最后，分类器需要判断新闻内容与评论证据组合起来更支持真实还是虚假。

模型再次构造四路关系：

```text
新闻 BERT 全局表示
评论证据向量
二者的绝对差
二者的逐元素乘积
```

这一步与前面的新闻--单评论关系打分类似，但比较对象已经从“一条评论”变成了“整个入选评论集合形成的证据”。

对应的分类关系向量为：

$$
\mathbf{z}_{n}^{\mathrm{cls}}
=\Phi(\mathbf{g}_{n}^{x},\mathbf{e}_n).
$$

四部分关系输入分类 MLP，输出两个 logits，分别对应真实新闻和虚假新闻。最后通过 Softmax 转换成两个类别的预测概率，并选择概率更高的类别作为最终结果。

$$
\widehat{\mathbf{y}}_n
=\mathrm{Softmax}
\left[
\mathrm{MLP}_{\mathrm{cls}}
(\mathbf{z}_{n}^{\mathrm{cls}})
\right].
$$

如果一条新闻完全没有有效评论，评论证据向量取零向量。由于分类关系中仍然保留新闻自身的 BERT 表示，模型仍然可以主要根据新闻正文完成判断。

## 十、整个架构的核心逻辑

把整个方法压缩成一句话，可以表述为：

> 本文首先利用 BERT、多尺度 CNN、尺度注意力和门控残差构造新闻与评论的选择表示，再由新闻及其评论集合共同预测新闻级选择分界，从而形成具有最低证据保障的变长评论子集；最后将入选评论按门控强度聚合为集合级证据，并与新闻全局表示联合完成真假分类。

这套架构有三个主要特点。

第一，选择是新闻相关的。同一条评论面对不同新闻可以得到不同的关系分数。

第二，分界是新闻相关的。不同新闻不再共享一个固定阈值，而是根据新闻、评论集合和评论数量产生自己的分界。

第三，评论集合既有离散选择，也有连续加权。分界决定评论是否进入集合，归一化权重决定入选评论在最终证据中的贡献大小。

## 十一、结束语

以上就是本文模型的方法架构。模型并不是简单地把全部评论平均，也不是对每条新闻固定选择相同数量的评论，而是先理解单条评论中的局部和全局语义，再结合当前新闻和整组评论预测选择分界，最后形成数量可变的证据子集。

这样设计的目的，是让分类器尽量关注与当前新闻真正相关的评论证据，同时减少无关评论对真假判断的干扰。

## 十二、老师可能追问的问题

### 1. 自适应选择是不是固定 Top-k？

不是。模型先根据每条新闻自己的分界自然选择评论，选择数量可以变化。最低证据数 3 只在自然选择不足时补选，不会把超过 3 条的结果裁剪掉。

### 2. 为什么需要把评论数量比例输入分界网络？

因为相似的评论集合平均语义可能来自不同规模的评论集合。加入 M_n/M 后，模型可以区分“少量评论形成的摘要”和“大量评论形成的摘要”，从而调整选择分界。

### 3. 为什么选择器使用 CNN，分类器却使用 BERT CLS？

选择器需要识别否定、质疑和事实片段等局部线索，因此加入多尺度 CNN；分类器处理的是已经组织好的集合级证据，更需要稳定的全局语义，因此使用 BERT `[CLS]`。这是功能分工，而不是两个模块复杂程度不一致。

### 4. q_ni 和 w_ni 有什么区别？

q_ni 表示评论超过当前新闻分界的程度；w_ni 是只在入选评论内部归一化后的相对贡献。前者用于选择和衡量证据强度，后者用于实际聚合评论向量。

### 5. 这里能否称为“最佳评论子集”？

更严谨的说法是“对真假分类任务最有利的评论证据子集”。模型没有人工标注的评论质量标签，而是根据最终新闻分类目标学习评论关系和选择分界，因此这里的“最佳”是任务驱动意义上的最佳，而不是人工语义评价意义上的绝对最佳。

### 6. R-Drop 属于这个模型架构吗？

不属于前向架构。R-Drop 是用于提高模型稳定性的训练正则化方法，不会增加推理模块。方法架构图中只需要画一次模型前向过程，不需要画两个 R-Drop 分支。

### 7. 大模型生成的纯干扰评论在推理时是否需要？

不需要。它们只用于构造评论选择相关的辅助数据，不进入推理流程。实际使用时，模型只需要新闻正文和原始用户评论。
