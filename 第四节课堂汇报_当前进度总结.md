# 第四节课方法改造：当前进度总结

## 1. 研究目标

本轮工作根据《第四节课课堂笔记》对原假新闻检测系统进行评论级改造，目标分为两部分：

1. 大语言模型生成“纯干扰评论”和“纯立场评论”，作为已知无用负例，训练评论选择模块识别无用评论。
2. 大语言模型为进入分类模块的原评论生成一一对应的去情绪化版本，原评论和去情绪化评论共同训练真假新闻分类模块。

新闻正文始终保持不变。去情绪化对象是评论，不是新闻正文；也不是先选择评论再去情绪化，而是预先为原评论建立第二视图。

## 2. 已完成的数据工作

### 2.1 选择器负例

- 已复用 100 条新闻的大模型负例生成结果。
- 每条新闻候选生成 3 条纯干扰评论和 2 条纯立场评论。
- 共得到 500 条候选，去重后实际加入 494 条。
- 覆盖 100 个训练样本，没有坏记录。

### 2.2 评论去情绪化

- 从已有 100 条新闻 ID 中筛出 84 条有评论的新闻。
- 对进入模型的前 32 条原评论生成去情绪化版本，共处理 1913 条评论。
- 首轮验证通过 1809 条；第一次重试通过 76 条；第二次重试通过 3 条。
- 最终通过 1888/1913 条，通过率约 98.69%。
- 剩余 25 条未通过评论不采用改写，第二视图自动回退为原评论。
- 写回训练集时 1888 条全部按 `sample_id + comment_index` 正确落位，错位数为 0。

### 2.3 最终训练数据

- 训练集：6120 条。
- Dev：875 条，保持 clean 原始分布。
- Test：1748 条，保持 clean 原始分布，目前没有用于新版方法调参。
- 有去情绪化评论的训练新闻：84 条。
- 有效去情绪化评论：1888 条。
- 有选择器负例的训练新闻：100 条。
- 选择器负例：494 条。
- 评论、类型、标签、去情绪化文本和验证标记五组字段已经检查为严格对齐。

## 3. 模块设计、损失函数与选择理由

### 3.1 共享文本编码模块

**输入：** 新闻正文、原评论、去情绪化评论以及生成负例。

**设计：** 使用同一个 Chinese BERT-wwm-ext 编码器获得新闻表示和评论表示。新闻正文始终保持原文。原评论和去情绪化评论使用同一个编码空间，便于比较和融合。

**交互表示：** 对新闻表示 $h^x$ 与评论表示 $h^c_j$ 拼接以下四部分：

```text
[新闻表示；评论表示；二者绝对差；二者逐元素乘积]
```

绝对差描述二者区别，逐元素乘积描述相似和匹配关系，比只拼接新闻与评论能提供更明确的新闻--评论交互信息。

**为什么选择共享编码器：** 数据规模有限，共享参数能够减少模型规模，并让原评论、去情绪化评论和负例处于可比较的语义空间。生成负例独立分支目前采用 `no_grad` 编码，避免选择器辅助任务反向干扰分类编码器。

### 3.2 动态评论选择模块

**输入：** 新闻--原评论交互表示。

**输出：** 每条原评论的 sigmoid 保留概率 $g_j\in[0,1]$。

**设计：** 选择器由两层 MLP 构成。训练时可以使用连续概率加权评论；验证和推理时按阈值形成动态评论子集。若一条有评论的新闻没有任何评论超过阈值，则使用 `min_selected=1` 补选概率最高的一条。

**为什么不用固定 top-k：** 不同新闻拥有的有效评论数量不同。固定选择相同数量会迫使模型保留无用评论，或者丢弃新闻中额外的有效证据。动态阈值更符合“不定长有效评论集合”的任务设定。

### 3.3 原评论与去情绪化评论分类模块

**输入：** 最多 32 条原评论，以及与它们逐位置对应的去情绪化评论。

**数据回退：** 通过验证的位置使用去情绪化文本；未通过验证或没有生成结果的位置直接使用原评论，因此两个视图的数量、顺序和位置完全一致。

**证据融合：** 选择器只根据原评论计算一组门控权重，同一组权重分别融合：

```text
原评论证据向量
去情绪化评论证据向量
```

使用相同权重的原因是保证两个视图比较的是同一批评论位置，避免“原评论选一组、去情绪化评论又选另一组”造成额外混杂。

**分类损失：** 对拥有有效第二视图的样本，当前采用两个交叉熵分类损失的平均：

$$
\mathcal{L}_{cls}
=\frac{1}{2}\left[
CE(f(x,C),y)+CE(f(x,\widetilde C),y)
\right].
$$

没有有效去情绪化评论的样本只计算原评论视图的交叉熵。

**为什么选择交叉熵：** 新闻真假是二分类任务，并且训练集提供明确的新闻标签，交叉熵与任务监督直接对应。两个视图平均可看作评论级数据增强：要求原评论和弱化情绪后的评论都能够支持正确新闻判断。

**额外一致性损失：** 代码还支持：

- 证据向量余弦距离损失 $\mathcal{L}_{sem}$；
- 两个视图预测分布的对称 KL 损失 $\mathcal{L}_{pred}$。

选择它们的初衷是防止去情绪化后语义或预测发生较大变化。但实验中权重各为 0.2 时，seed 44 的 Macro-F1 从 0.946214 降至 0.934734，说明约束过强。因此当前默认权重为 0，只保留为后续小权重消融，而不是最终默认配置。

### 3.4 生成负例与半监督评论选择模块

**输入：** 原评论属于未标注集合 $U$；LLM 生成的纯干扰和纯立场评论属于可靠负例集合 $N$。

**为什么是半监督：** 我们只确定生成负例是无用评论，却不能把所有原评论都标为有用。原评论中仍可能包含转发、情绪表达和弱相关内容。因此不能直接使用“原评论全为正例、生成评论全为负例”的普通二分类 BCE，否则会引入错误正标签。

**当前采用的思路：** 使用负例--未标注排序损失，使生成负例分数低于同新闻中高置信度原评论：

$$
\mathcal{L}_{sel}
=\frac{1}{|N|}\sum_{n\in N}
[m+s_n-s_{u^*}]_+,
$$

其中 $s_n$ 是生成负例得分，$s_{u^*}$ 是同新闻中最高分原评论得分，$m$ 是排序间隔。

**为什么选择排序而不是负例-only BCE：** 如果只告诉模型“这些负例应为 0”，却没有任何正向或相对参照，最简单的解可能是把所有评论都判为低分。排序损失至少要求负例相对原评论更低，而不是要求所有原评论都具有固定正标签。

**物理独立分支：** 当前代码将最多 5 条生成负例放入独立张量分支：

- 不占用最多 32 条原评论槽位；
- 不进入评论证据融合和真假分类；
- 负例编码采用 `no_grad`，不更新共享 BERT；
- 使用独立 RNG 上下文，不改变分类路径 dropout 的随机状态；
- 辅助损失只用于选择器训练。

**最新诊断修改：** 双向排序梯度曾导致模型主要抬高原评论分数来满足间隔，原评论保留率从 46.72% 升至 73.12%，但 Macro-F1 降至 0.930204。因此最新本地代码把原评论锚点设为 stop-gradient，使损失只能压低生成负例。这一修改通过了梯度单元测试，但尚未完成服务器重训练。

### 3.5 联合损失

两个任务可以组成同一个总损失，但不适合强行使用同一种子损失：

$$
\mathcal{L}_{total}
=\mathcal{L}_{cls}
+\lambda_{sel}\mathcal{L}_{sel}
+\lambda_{sem}\mathcal{L}_{sem}
+\lambda_{pred}\mathcal{L}_{pred}.
$$

- $\mathcal{L}_{cls}$ 使用新闻真假标签，服务于分类模块；
- $\mathcal{L}_{sel}$ 使用“可靠负例 + 未标注原评论”，服务于评论选择模块；
- $\mathcal{L}_{sem}$ 和 $\mathcal{L}_{pred}$ 是可选的双视图一致性项。

目前默认 $\lambda_{sem}=\lambda_{pred}=0$。选择器损失权重仍需在最终梯度解耦后重新验证。

### 3.6 老师要求的目标梯度流

用户最终确认的目标是：

```text
原评论 + 去情绪化评论
        ↓
真假分类损失
        ↓
只训练共享编码器与分类模块

原评论 + LLM生成负例
        ↓
半监督选择器损失
        ↓
只训练评论选择模块
```

最后将两个子损失相加为一个联合训练目标。这里“同一个损失函数”应理解为一个总目标中的两个子损失，而不是让新闻二分类和评论选择强行共享完全相同的标签与交叉熵形式。

### 3.7 当前代码与目标架构的差别

**数据和张量分支已经符合要求：**

- 原评论与去情绪化评论用于双视图分类；
- 生成负例独立存放，不进入分类证据；
- 原评论、第二视图和负例的槽位已经物理分离。

**梯度职责尚未完全符合要求：**

1. 当前分类证据权重直接来自 selector，尚未执行 `detach()`。因此原评论和去情绪化评论产生的分类损失会通过门控权重反向更新 selector。也就是说，它们目前不只是训练分类模块，还会间接训练选择模块。
2. 最新 stop-gradient 排序版本中，原评论得分只作为无梯度锚点，排序损失只压低生成负例。因此它又没有完全做到“原评论和生成负例共同更新选择器”。

所以目前的准确判断是：

> 数据层面的分工已经正确，但模型的最终梯度分工仍未完全实现老师要求。此前实验属于定位梯度耦合问题的中间实验，不能视为最终目标架构的正式结果。

若后续继续修改，需要：

1. 分类时使用 `selector_weights.detach()`，切断分类损失到 selector 的梯度；
2. 重新设计真正使用未标注原评论与可靠负例的 PU/负例--未标注选择损失；
3. 保持负例分支不更新共享 BERT；
4. 使用 $\mathcal{L}_{cls}+\lambda_{sel}\mathcal{L}_{sel}$ 联合训练并重新做同种子配对实验。

## 4. 主要代码职责

### 核心模型

- `src/config.py`：保存训练参数，包括原评论槽位数、selector-only 负例槽位数和各损失权重。
- `src/data.py`：读取 JSONL；对齐原评论、去情绪化评论和验证标记；构造最多 32 条原评论分类张量及最多 5 条独立负例张量。
- `src/model.py`：实现正文基线、动态评论选择、评论证据融合、双视图分类、选择器负例排序损失及一致性损失。
- `src/train.py`：设置随机种子、构造 DataLoader、训练与 Dev 评估、保存 Dev Macro-F1 最佳 checkpoint。

### 大模型数据生成与验证

- `scripts/prepare_llm_augmentation.py`：生成纯干扰/纯立场负例请求。
- `scripts/prepare_comment_neutralization.py`：为原评论生成去情绪化请求，严格要求数量和顺序一致。
- `scripts/run_deepseek_augmentation.py`：调用 DeepSeek API，支持断点续跑、并发、错误记录和两种任务的输出校验。
- `scripts/validate_comment_neutralization.py`：用 Chinese BERT 检查语义相似度，并检查前后立场、否定词和不确定性表达。
- `scripts/merge_comment_neutralization.py`：按样本和评论位置合并多轮验证通过结果。
- `scripts/apply_comment_neutralization.py`：把验证通过的去情绪化评论写回原训练集对应位置。
- `scripts/apply_llm_augmentation.py`：追加生成负例并保持所有评论字段严格对齐；只对新增负例去重，不删除重复的真实评论。

### 分析与文档

- `scripts/analyze_selector.py`：统计保留概率、阈值以上数量、强制补选数量和各评论类型的选择行为。
- `scripts/export_evidence.py`：导出模型最终保留的评论证据，用于案例分析。
- `README.md`：完整实验流程和命令。
- `第四节课代码改造记录.md`：记录方法理解修正、代码改造和历次实验诊断。
- `overleaf_methodology.tex`：论文方法章节草稿。

## 5. 已获得的实验结果

### 5.1 原评论配对基准（Dev Macro-F1）

| Seed | 原评论基准 |
|---|---:|
| 42 | 0.943971 |
| 43 | 0.939302 |
| 44 | 0.945074 |
| 平均 | 0.942782 |

### 5.2 早期完整方法（旧数据流）

| Seed | 完整方法 | 与同种子基准差值 |
|---|---:|---:|
| 42 | 0.945055 | +0.001084 |
| 43 | 0.943858 | +0.004556 |
| 44 | 0.932368 | -0.012706 |
| 平均 | 0.940427 | -0.002355 |

前两个种子有小幅提升，但 seed 44 明显退化，所以不能只报告最好结果，也不能认为当前完整方法已经稳定有效。

### 5.3 seed 44 消融诊断

| 配置 | Dev Macro-F1 | 评论保留率 |
|---|---:|---:|
| 原评论基准 | 0.945074 | 89.41% |
| 去情绪化双视图 + 一致性权重 0.2/0.2 | 0.934734 | 32.88% |
| 去情绪化双视图 + 一致性权重 0/0 | 0.946214 | 46.72% |
| 双视图 + 旧负例数据流，负例权重 0 | 0.939337 | 53.81% |
| 双视图 + 旧负例数据流，负例权重 0.01 | 0.930140 | 29.34% |
| 双视图 + 旧负例数据流，负例权重 0.1 | 0.936865 | 22.93% |
| 仅 `evidence_mask` 隔离负例 | 0.934759 | 47.41% |
| 物理双分支负例，双向排序梯度 | 0.930204 | 73.12% |

这里最重要的积极结果是：去情绪化评论双视图在关闭两个额外一致性损失后，seed 44 达到 0.946214，略高于原评论基准 0.945074。说明去情绪化评论本身不是无效的，主要困难集中在一致性约束和负例监督方式上。

## 6. 遇到的困难与对应修改

### 困难一：最初误解去情绪化对象

早期把新闻正文进行了去情绪化。用户进一步澄清后确认，课堂笔记要求的是评论去情绪化。

修改：删除正文去情绪化主流程，改为原评论级一一对应双视图；新闻正文始终不变。

### 困难二：API 返回空字符串、列表缺项和 ID 改写

DeepSeek 有时把表情、`@用户名` 或极短评论改为空字符串，省略列表位置，或者擅自缩短重试请求 ID。

修改：无法去情绪化的评论自动回退原文；提示词明确不能省略、合并或输出空字符串；输入请求 ID 成为权威 ID；支持断点续跑和逐条错误保存。

### 困难三：自动验证存在严格失败

1913 条评论中部分改写语义相似度不足或改变否定/不确定性提示。

修改：进行了两轮定向重生成，最终通过 1888 条；仍失败的 25 条安全回退原评论。

### 困难四：追加负例时评论视图错位

脚本先删除空评论再截取其他字段，导致去情绪化标记位置偏移；同时曾错误地对所有评论去重，删除了重复真实评论。

修改：先按原始长度对齐全部字段，再逐位置过滤；保留全部真实评论，只对新增负例去重。有效去情绪化评论从错误的 1785 条恢复到 1888 条。

### 困难五：一致性损失过强

`semantic_loss_weight=0.2` 与 `stance_loss_weight=0.2` 在 seed 44 上使 Macro-F1 从 0.946214 降至 0.934734。

修改：保留原评论/去情绪化评论共同分类，但把两个额外一致性项默认设为 0，后续如继续实验只测试更小权重。

### 困难六：生成负例污染分类证据

旧实现把负例排到前 32 条评论中，不仅挤掉真实评论，还直接参与真假分类。即使负例损失权重为 0，性能仍下降。

修改过程：

1. 先增加 `evidence_mask`，让负例融合权重为零；但负例仍与原评论一起经过共享 BERT，训练态计算路径仍发生变化。
2. 再改为物理双分支：32 条原评论用于分类，5 条负例单独编码，只用于排序。
3. 隔离测试证明，开启负例分支后分类 logits、共享 BERT 梯度、分类器梯度和 RNG 状态都与关闭负例分支时完全一致，只有 selector 头梯度变化。

### 困难七：排序损失通过抬高原评论取巧

物理双分支后，seed 44 的原评论保留率仍从 46.72% 升至 73.12%，性能降至 0.930204。说明双向排序梯度主要通过抬高原评论最高分来满足间隔。

最新修改：对原评论最高分锚点执行 stop-gradient，排序损失只允许压低生成负例分数。梯度单元测试已通过，但尚未在服务器完成新的训练实验。

## 7. 当前能够得出的结论

1. 评论级数据流程已经建立完整：生成、验证、重试、合并、写回和字段对齐均已完成。
2. 去情绪化评论双视图具有潜力；至少在 seed 44、无额外一致性损失时略高于同种子原评论基准。
3. 强一致性损失 0.2/0.2 不合适，会明显压低性能。
4. 生成负例是否真正提高选择器能力，目前还没有得到正面结论。旧实现存在分类污染，新实现又暴露了排序目标取巧问题。
5. 最新 stop-gradient 排序版本尚未完成服务器训练，因此不能把它作为已验证有效方法汇报。
6. 当前代码的数据分支已经符合目标，但分类损失仍会通过门控权重更新 selector，最新排序损失又只从负例侧更新，因此最终梯度职责尚未完全符合“分类数据只训练分类模块、原评论与负例训练选择模块”的要求。
7. 当前不应报告 test 结果，也不应选择单个最好 seed 宣称方法提升；应完成梯度解耦后，再坚持同种子配对和多随机种子 Dev 比较。

## 8. 汇报时可使用的简短表述

“目前我们已经完成了课堂笔记两条思路所需的评论级数据流程：一类是生成无用评论用于选择器，另一类是为原评论生成去情绪化第二视图用于分类器。我们处理了 1913 条评论，三轮验证后有 1888 条通过，未通过的安全回退原文。实验发现，去情绪化双视图本身有一定潜力，但较强的一致性约束会降低效果；负例监督则暴露了挤占真实评论、污染分类路径和排序目标取巧等问题。进一步检查梯度流后发现，当前数据分支已经分开，但分类损失仍会通过门控间接训练 selector，而最新负例损失又只从负例侧更新，因此尚未完全实现老师要求的梯度职责分离。当前阶段的主要成果是完成数据构造、定位架构和损失问题，并明确了下一步最终梯度解耦方案，还不能宣称完整方法已经稳定超过基准。”
