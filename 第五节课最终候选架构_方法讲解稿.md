# 本文模型方法讲解稿

> 对应文件：`overleaf_methodology_current_neutral_only_4pages.tex`  
> 建议时长：12—15 分钟  
> 当前口径：Dev 集已冻结最终候选，尚未使用 Test 集；模型名称暂不确定，统一称为“本文模型”或“所提方法”。

## 一、开场与研究任务（约 1 分钟）

各位老师好，下面我介绍当前模型的方法部分。

本文研究的是结合新闻正文和用户评论的假新闻检测。对每一条新闻，模型不仅输入新闻正文，还输入最多 32 条评论，最终输出该新闻属于真实新闻还是虚假新闻。

当前方法主要包含两个连续步骤。第一个步骤是在一条新闻的多条评论中计算每条评论的证据分数，并形成一个数量可变的评论集合；第二个步骤是把这些评论聚合成新闻级评论证据，再与新闻正文联合完成真假分类。

训练阶段还引入了评论去情绪化视图。也就是说，部分原评论具有一个语义和立场基本不变、但情绪表达被弱化的对应版本。原评论和去情绪化评论共同训练分类模块，但模型在验证和推理时只需要新闻正文与原评论，不需要额外生成去情绪化评论。

## 二、输入数据与符号（约 1.5 分钟）

首先说明输入形式。

对于第 $n$ 条新闻，$x_n$ 表示新闻正文，$y_n$ 表示真假标签。本文约定 0 为真实新闻，1 为虚假新闻。

$\mathcal{C}_n^o$ 表示原评论集合，$\mathcal{C}_n^u$ 表示与原评论逐位置对齐的去情绪化评论集合。上标 $o$ 是 original，上标 $u$ 表示去情绪化视图。每条新闻最多保留 32 个评论槽位，因此公式中的 $M$ 固定为 32。

有些新闻实际评论数少于 32，所以使用 $m_{ni}$ 表示第 $i$ 个位置是否为有效评论。如果该位置存在评论，$m_{ni}=1$；如果只是 padding，$m_{ni}=0$。

$\delta_n$ 表示当前样本是否至少包含一条验证通过的去情绪化评论。若 $\delta_n=1$，训练时同时计算两个评论视图；若 $\delta_n=0$，只计算原评论视图。

去情绪化评论与原评论严格按位置对齐。某个位置的改写只有通过语义、立场和否定关系检查后才会使用；如果没有改写，或者改写没有通过验证，该位置直接回退为原评论。因此两个视图的评论数量、顺序和掩码始终相同。

这里还需要说明当前最终模型的数据口径。训练文件中虽然保留了 494 条大模型生成的纯干扰和纯立场评论，但最终候选把选择器负例数设置为 0，同时关闭负例排序损失。因此这 494 条评论不进入最终模型的证据集合，也不参与最终选择器训练，只保留为前期消融实验数据。

## 三、总体计算流程（约 1 分钟）

整个模型可以从前向传播的顺序理解为五个阶段。

第一，使用同一个 Chinese BERT-wwm-ext 编码新闻和评论。

第二，在评论选择分支中，把 BERT 的完整 token 序列送入卷积核为 2、3、4 的多尺度 CNN，得到三个尺度的局部表示。

第三，通过尺度注意力决定当前文本更依赖哪一种局部尺度，再把局部表示与 BERT 的全局表示进行门控残差融合。

第四，对每一个新闻—评论对计算关系向量，通过选择 MLP 得到该评论的连续证据分数。训练时采用软门控，推理时按照阈值 0.20 形成硬评论子集。

第五，使用这些评论权重聚合评论的 BERT 全局表示，再与新闻全局表示共同输入分类 MLP，输出真假预测。

训练阶段如果样本具有有效的去情绪化评论，就复用同一组评论权重再构造一个去情绪化证据向量，通过同一个分类器计算第二条分类路径。

## 四、共享 BERT 编码（约 1 分钟）

下面介绍每一个模块。

新闻正文和每条评论都输入同一套 Chinese BERT-wwm-ext。BERT 对新闻输出矩阵 $\bm{H}_n^x$，对第 $i$ 条原评论输出矩阵 $\bm{H}_{ni}^o$。这里的 $\bm{H}$ 包含序列中每个 token 的上下文表示。

新闻最大长度是 256，评论最大长度是 96，BERT 隐藏维度 $d$ 是 768。矩阵的第 0 个位置，也就是 `[CLS]`，分别记为 $\bm{g}_n^x$ 和 $\bm{g}_{ni}^o$。

同一份 BERT 输出进入两条后续路径。评论选择路径读取完整的 token 序列，因为后面还要提取局部短语特征；真假分类路径直接使用 `[CLS]`，把它作为新闻和评论的全局语义表示。

实现时，一个 batch 内的评论会先从 $[B,M,L_c]$ 展平为 $[BM,L_c]$ 统一送入 BERT，再恢复成按新闻组织的 $[B,M,d]$。这里 $B$ 是 batch size，$M$ 是 32，$L_c$ 是评论长度 96。

## 五、多尺度 CNN 与尺度注意力（约 2.5 分钟）

选择分支首先在 BERT 上下文序列上执行多尺度一维卷积。卷积核大小分别为 2、3 和 4，每个尺度有 128 个输出通道。

以评论为例，卷积核大小为 2 时，模型在相邻两个 token 的上下文表示上提取局部组合；卷积核为 3 和 4 时，对应提取更长的局部模式。卷积输出经过 GELU 激活，再沿序列维做带掩码的最大池化，得到三个 128 维尺度向量。

公式中的 $\bm{A}_{nik}^o$ 表示第 $k$ 个卷积分支仍然保留序列位置的输出，$\bm{u}_{nik}^o$ 表示最大池化后的固定长度向量。padding 位置在卷积前被置零，完全由 padding 构成的卷积窗口也会被屏蔽，所以补齐位置不会成为错误的最大值。

三个 CNN 输出随后分别通过独立线性层，从 128 维投影到 BERT 的 768 维空间，投影结果记为 $\bm{v}_{nik}^o$。这里的下标 $k$ 表示 2、3、4 三种尺度。

得到三个尺度向量以后，模型没有直接把它们简单拼接，而是计算尺度注意力。模型先对三个尺度求平均，再将尺度均值与评论的 `[CLS]` 拼接，经过一个带 Tanh 的线性层生成查询向量 $\bm{q}_{ni}^o$。

然后，查询向量分别与三个尺度向量做缩放点积，并在尺度维度上执行 Softmax，得到权重 $\alpha_{nik}^o$。三个权重之和为 1。最终局部表示 $\bm{l}_{ni}^o$ 就是三个尺度投影按照 $\alpha$ 加权求和的结果。

因此，这里的尺度注意力发生在一条文本内部。它决定的是当前新闻或评论更依赖 2-gram、3-gram 还是 4-gram 局部模式，还没有进行评论之间的筛选。

## 六、门控残差融合（约 1 分钟）

尺度注意力得到的局部表示经过线性层、GELU 和 dropout，形成变换后的局部向量 $\widetilde{\bm{l}}_{ni}^o$。

接着，模型把这个局部向量与评论的 BERT `[CLS]` 拼接，通过 Sigmoid 生成一个 768 维门控向量 $\bm{a}_{ni}^o$。门控中的每个维度都在 0 到 1 之间，用来控制该维度需要注入多少 CNN 局部信息。

最后，把门控后的局部向量加到原始 `[CLS]` 上，再进行 Layer Normalization，得到评论选择表示 $\bm{r}_{ni}^o$。新闻正文经过完全相同的模块，得到新闻选择表示 $\bm{r}_n^x$。

所以，$\bm{g}$ 表示原始 BERT 全局表示，$\bm{l}$ 表示尺度注意力后的局部表示，$\bm{a}$ 表示逐维融合门，$\bm{r}$ 才是送入评论选择器的最终表示。

## 七、新闻—评论交互与连续证据分数（约 1.5 分钟）

得到新闻和评论的选择表示后，模型对每一个新闻—评论对构造四路交互特征。

四路特征依次是：新闻表示本身、评论表示本身、二者的绝对差，以及二者的逐元素乘积。公式中使用 $\Phi$ 表示这个操作，输出维度是 $4d$，也就是 3072 维。

绝对差描述两个表示在哪些维度上存在差异，逐元素乘积描述它们在哪些维度上同时激活。四路关系向量输入两层选择 MLP，先从 $4d$ 映射到 $d$，再输出一个标量选择 logit $s_{ni}$。

选择 logit 经过 Sigmoid 和有效评论掩码以后，得到连续证据分数 $p_{ni}$。选择温度 $T$ 当前设为 1。有效评论的分数位于 0 到 1 之间，padding 位置的分数恒为 0。

这里需要注意，$p_{ni}$ 是相对于当前新闻计算的，不是脱离新闻后单独判断一条评论是否重要。因此，同一条评论面对不同新闻时可以得到不同分数。

## 八、训练软门控与推理硬选择（约 2 分钟）

评论选择在训练和推理阶段使用两种不同的组织方式。

训练时不直接按照阈值删除评论，而是把 $p_{ni}$ 转换成连续聚合权重 $w_{ni}^{\mathrm{tr}}$。当一条新闻的评论分数总和大于等于 1 时，权重按照分数总和归一化；当总和小于 1 时，分母固定为 1。

后一种情况意味着权重和可以小于 1。如果所有评论的证据分数都很低，最终评论证据向量的幅度也会相应减弱，而不是经过普通归一化后又被强制放大。由于训练阶段保留连续权重，分类损失可以通过评论聚合反向传播到选择 MLP、多尺度 CNN 和共享 BERT。

验证和推理阶段采用硬选择。模型使用统一阈值 $\tau=0.20$，保留所有满足 $p_{ni}\geq0.20$ 的有效评论，形成动态集合 $\mathcal{S}_n$。不同新闻最终保留的评论数可以不同，因此这里不是固定 top-k。

如果一条新闻原本有评论，但没有任何评论达到阈值，模型会补选分数最高的一条；如果新闻本身没有评论，则评论集合保持为空。

硬集合之外的评论权重被置为 0，集合内部仍然按照连续分数重新归一化。也就是说，阈值负责决定“保留哪些评论”，连续分数负责决定“保留后的评论分别占多大权重”。

## 九、评论证据聚合与真假分类（约 1 分钟）

选择器确定评论权重后，分类分支使用评论的 BERT `[CLS]` 进行加权求和，得到原评论证据向量 $\bm{e}_n^o$。

这里没有使用选择分支经过 CNN 的 $\bm{r}_{ni}^o$ 作为最终分类表示，而是使用更直接的 BERT `[CLS]`。因此，CNN、尺度注意力和门控残差主要负责计算评论的重要程度；真正聚合进真假分类器的是评论的全局语义表示。

如果一条新闻没有任何有效评论，评论证据向量就是零向量，新闻 `[CLS]` 仍然可以直接进入后续分类关系。

模型再次使用四路交互函数，把新闻 `[CLS]` 与评论证据向量组合成 $4d$ 维关系向量，再通过两层分类 MLP 输出两个 logits，分别对应真实和虚假类别，最后通过 Softmax 得到预测分布。

## 十、去情绪化双视图训练（约 2 分钟）

去情绪化视图只进入分类路径，不重新运行选择路径。也就是说，模型不会针对去情绪化评论重新计算 CNN、尺度注意力或选择分数。

共享 BERT 对去情绪化评论或者回退后的原评论重新编码，得到第二组评论 `[CLS]` 表示 $\bm{g}_{ni}^u$。然后直接复用原评论视图产生的同一组权重 $w_{ni}$，得到去情绪化证据向量 $\bm{e}_n^u$。

这样，原评论证据和去情绪化证据使用的是相同评论位置、相同有效掩码和相同重要度，两个证据向量之间的差异只来自评论文本表达的变化。

第二个证据向量与同一个新闻 `[CLS]` 构造四路关系，并通过同一个分类 MLP 输出 $\bm{o}_n^u$。两个视图共享 BERT、评论权重和分类器参数。

此外，第二视图中的评论权重没有停止梯度。因此，去情绪化视图的分类损失既更新分类器和共享 BERT，也可以通过权重反向影响原评论选择模块。

对于没有有效去情绪化评论的样本，也就是 $\delta_n=0$，只计算原评论视图的交叉熵。对于 $\delta_n=1$ 的样本，原评论和去情绪化评论的交叉熵各占一半。最后对一个 batch 内所有样本的逐样本损失取平均，得到总损失 $\mathcal{L}$。

当前最终候选不加入负例排序损失、显式稀疏损失、证据余弦一致性损失和预测分布 KL 损失，所以这些附加项的权重都为 0。最终训练目标就是新闻级单视图或双视图交叉熵。

## 十一、训练、验证与推理设置（约 1 分钟）

模型使用 AdamW 端到端训练 3 个 epoch，batch size 为 4，学习率为 $2\times10^{-5}$，weight decay 为 0.01，dropout 为 0.1。训练前 10% 的步数进行线性 warmup，梯度范数裁剪阈值为 1.0。

每个 epoch 结束后在 Dev 集计算 Macro-F1 等指标并保存候选 checkpoint。训练结束后，再使用统一的 Dev 阈值网格复评三个随机种子的候选模型，最终将阈值 0.20 冻结为推理操作点。

推理时只需要新闻和原评论。模型依次计算评论分数、构造硬评论集合、执行最低一条补选、聚合评论证据并输出真假分类。同时还可以导出每条评论的证据分数、选择掩码和归一化权重，用于后续案例分析。

目前的准确实验状态是：最终候选已经根据 Dev 集冻结，但尚未读取 Test 集。因此当前只能报告 Dev 阶段的架构选择和验证结果，不能把它表述为最终测试性能。

## 十二、总结（约 40 秒）

最后总结一下本文模型的数据流。

模型先使用共享 BERT 获得新闻和评论的上下文表示；选择分支通过多尺度 CNN、尺度注意力和门控残差形成选择表示；新闻—评论四路交互网络输出连续证据分数；训练时使用软权重保持端到端可微，推理时使用阈值 0.20 得到数量可变的硬评论集合；分类分支再将评论 `[CLS]` 聚合为新闻级证据，与新闻 `[CLS]` 共同完成真假分类。

在此基础上，训练阶段对少量具有验证通过改写的样本增加去情绪化评论视图，并复用原评论选择权重进行双视图分类。最终模型不使用生成负评论的半监督排序分支，也暂不使用额外一致性正则。

我的方法部分介绍完毕。

---

## 讲解时容易说错的口径

1. 不要说“最终模型使用 494 条负评论训练选择器”。最终配置没有使用它们。
2. 不要说“所有训练评论都有去情绪化版本”。只有 84 条训练新闻具有验证通过的去情绪化评论。
3. 不要说“推理时也需要大模型生成去情绪化评论”。去情绪化视图只在训练阶段使用。
4. 不要把尺度注意力和评论选择混为一谈。尺度注意力在单条文本内部选择 2、3、4 三种局部尺度；评论选择发生在一条新闻对应的多条评论之间。
5. 不要说推理使用固定 top-k。当前方法使用阈值动态选择，只在集合为空时补选最高分评论。
6. 不要说 CNN 输出直接进入真假分类器。CNN 融合表示用于选择器，分类证据使用评论 BERT `[CLS]`。
7. 不要说已经获得 Test 结果。当前状态是 Dev 冻结最终候选，Test 尚未运行。

## 可能被问到的问题与简短回答

### 1. 为什么训练使用软门控，推理使用硬选择？

训练时软门控保留连续梯度，使新闻级分类误差能够更新评论选择模块；推理时硬阈值产生明确、数量可变的评论子集，便于控制保留率并导出证据。

### 2. 为什么分类分支不用 CNN 融合后的表示？

当前消融中，BERT-CNN 用于选择表示较稳定，而把它同时用于分类没有超过 BERT `[CLS]` 分类基线。因此最终配置将局部增强用于选择，把 BERT `[CLS]` 用于证据聚合和真假分类。

### 3. 为什么去情绪化视图复用原评论权重？

复用权重可以保证两个视图比较的是同一批评论位置，并把差异限制在评论表达变化上。如果两个视图分别选择评论，就会同时混入文本变化和评论集合变化。

### 4. 为什么最终没有使用纯干扰和纯立场评论？

相关实验能够降低部分生成负例的分数，但跨随机种子没有稳定提高 Macro-F1；某些设置还会使整体选择概率偏移。因此最终主模型保留效果更稳定的去情绪化双视图路径，把负例监督结构降为消融。

### 5. 阈值 0.20 是如何得到的？

候选 checkpoint 固定后，在同一 Dev 阈值网格上复评三个随机种子，综合 Macro-F1、评论保留率和强制补选情况后冻结为 0.20。该阈值不参与模型参数训练。

### 6. 当前方法最大的限制是什么？

去情绪化增强只覆盖 84 条训练新闻，覆盖范围仍然较小；最终候选也只在 Dev 阶段完成选择，尚未使用 Test 集进行最终一次评估。因此目前应把结论限定为架构在 Dev 上表现较稳定，而不是已经完成最终泛化验证。
