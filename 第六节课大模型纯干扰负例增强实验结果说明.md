# 大模型纯干扰负例增强实验结果说明

## 一、结论先行

本次实验成功完成了大模型纯干扰评论的生成、校验、数据写入和负例训练，但这些负例没有进一步提高新闻真假分类的 F1。

更准确地说：

- **数据增强流程成功**：1000 条新闻各生成 3 条纯干扰评论，共 3000 条，数据格式和路由均正确；
- **负例学习成功**：独立拒绝头确实接收到了负例梯度，并学会压低干扰评论；
- **主任务增益没有出现**：启用负例拒绝头后，Test Macro-F1 仍为 0.9553；
- **过度使用负例会产生副作用**：拒绝权重过大或取消主模型冻结，都会误删有效评论并降低 F1。

因此，本轮实验不是“负例模块完全失效”，而是“负例识别能力没有转化为新闻分类性能提升”。

## 二、实验目的

原模型通过新闻级自适应阈值，从每条新闻对应的评论中动态选取证据子集。为了进一步提升选择器过滤无用评论的能力，本轮实验加入大模型生成的纯干扰负例。

实验期望是：

```text
模型见过更多无用评论
        ↓
更准确地识别并过滤干扰评论
        ↓
保留更有价值的评论证据
        ↓
提高新闻真假分类 F1
```

本轮只使用纯干扰负例，不使用立场负例，也不使用去掉强标签的 clean 数据集。

## 三、数据增强情况

训练数据使用：

```text
data/weibo21_distractor_only_v7/train.jsonl
```

Dev 和 Test 使用：

```text
data/weibo21_release_v4/dev.jsonl
data/weibo21_release_v4/test.jsonl
```

增强数据统计如下：

| 项目 | 数量 |
|---|---:|
| 训练新闻 | 6120 |
| 加入负例的新闻 | 1000 |
| 每条增强新闻的负例 | 3 |
| 纯干扰负例总数 | 3000 |
| 立场负例 | 0 |

生成负例被单独放入 `selector_negative` 通道，不进入真假分类证据。因此，即使生成评论质量不理想，也不会直接作为新闻分类依据污染标签。

## 四、实验过程

### 4.1 第一阶段：高分 R-Drop 模型

首先训练了新闻级自适应选择器，并加入 R-Drop。最终 seed 42 单模型在推理时使用 `min_selected=3`：

| 数据集 | Macro-F1 | Accuracy | 评论保留率 |
|---|---:|---:|---:|
| Dev | 0.969120 | 0.969143 | 0.141273 |
| Test | 0.955347 | 0.955378 | 0.149504 |

但检查配置后发现，该阶段使用了：

```text
selector_loss_weight = 0
selector_rejection_weight = 0
```

这意味着训练文件中虽然包含 3000 条负例，但负例排序损失和拒绝修正都没有启用。因此，这一阶段的提升主要来自 R-Drop、自适应阈值和 `min_selected=3`，不能归因于负例增强。

### 4.2 第二阶段：只训练独立负例拒绝头

为了真正使用负例，同时保护已经取得较高 F1 的主模型，采用两阶段训练：

```text
冻结 BERT、主选择器、动态阈值和分类器
                    ↓
只训练独立拒绝头
                    ↓
拒绝头学习区分原评论和纯干扰负例
```

训练检查结果：

```text
最大负例排序损失 = 1.837
发生变化的拒绝头参数张量 = 3
拒绝头参数变化量 = 2.261
发生变化的非拒绝头参数 = 0
```

这些结果证明：负例确实产生了训练梯度，同时主模型参数保持不变。

在 Dev 上扫描 `one_sided_veto` 拒绝权重：

| 拒绝权重 | Dev Macro-F1 | 评论保留率 |
|---:|---:|---:|
| 0.00 | 0.969120 | 0.141273 |
| 0.05 | 0.969120 | 0.141210 |
| 0.10 | 0.967978 | 0.141147 |
| 0.25 | 0.967978 | 0.140957 |
| 0.50 | 0.966836 | 0.140386 |
| 0.75 | 0.966836 | 0.139880 |
| 1.00 | 0.965695 | 0.139690 |

结果表明，权重越大，模型过滤的评论越多，但 F1 逐渐下降。最终在 Dev 上冻结 `rejection_weight=0.05`，因为它是唯一在不降低 F1 的情况下实际启用负例拒绝头的非零权重。

冻结配置在 Test 上的结果：

| 设置 | Test Macro-F1 | Accuracy | 评论保留率 |
|---|---:|---:|---:|
| 原 R-Drop 模型 | 0.955347 | 0.955378 | 0.149504 |
| 负例拒绝头，权重 0.05 | 0.955347 | 0.955378 | 0.149343 |

负例拒绝头略微减少了评论保留量，但没有改变任何 Test 分类结果。

### 4.3 第三阶段：取消冻结并联合微调

为了判断负例是否能在全参数训练中发挥更大作用，又从负例拒绝头 checkpoint 初始化，取消冻结并对整个模型联合微调一轮。

结果如下：

| 模型 | Dev Macro-F1 | 评论保留率 | 平均动态阈值 |
|---|---:|---:|---:|
| 冻结主模型，只训练拒绝头 | 0.969120 | 0.141210 | 0.571203 |
| 取消冻结，联合微调 | 0.958842 | 0.139246 | 0.590702 |

联合微调后 Dev Macro-F1 下降约 1.03 个百分点。平均动态阈值明显升高，说明模型变得更加严格，开始过度过滤真实评论。即使推理时设置 `min_selected=3`，也没有恢复性能。

因此，联合微调模型被 Dev 淘汰，没有继续作为最终模型。

## 五、为什么负例学会了，但 F1 没有提高

可以用一个简单比喻理解：

> 我们想训练一个保安，让他找出“不能提供有效线索的人”。但训练时给他的负例都是非常明显的假访客。保安很快学会了识别这些假访客，可实际工作中面对的都是普通访客。部分说话方式像假访客的人，实际上可能掌握重要线索。保安检查得越严格，反而越可能把有用的人挡在门外。

对应到模型中，主要有以下原因。

### 5.1 纯干扰负例过于容易

大部分负例排序损失很快降到零，说明模型较容易区分生成干扰评论。这些负例没有覆盖当前选择器真正容易犯错的困难区域。

### 5.2 训练负例与真实评论存在分布差异

Dev 和 Test 中没有人工生成的纯干扰评论。拒绝头在真实推理阶段面对的是普通用户评论，而不是大模型生成文本，因此它学到的部分特征可能只是生成文本风格。

### 5.3 “不像生成负例”不等于“具有新闻判别价值”

负例训练能够告诉模型哪些评论像人工干扰，但不能直接告诉模型哪些真实评论最有助于判断新闻真假。原始评论中一些口语化、信息较少或轻微跑题的内容，仍可能提供有用信号。

### 5.4 拒绝权重越大，误删风险越高

小权重只改变极少数评论，因此 F1 不变；大权重虽然减少了更多评论，但也删除了部分有效证据，导致 F1 下降。

### 5.5 当前主选择器已经较强

R-Drop、自适应阈值和最少评论约束已经取得较高 F1。简单纯干扰负例能够提供的边际信息有限。

## 六、如何评价本轮实验

### 成功的部分

- 完成了1000条新闻、3000条纯干扰负例的数据增强；
- 负例与分类证据严格隔离，没有污染真假标签；
- 独立拒绝头确实学到了负例排序信息；
- 通过冻结策略避免了主模型性能下降；
- 验证了拒绝权重过大和全参数联合微调会导致过度过滤。

### 没有达到的部分

- 负例增强没有提高 Dev 或 Test Macro-F1；
- 评论保留率只出现非常小的下降；
- 取消冻结后性能明显下降；
- 不能将现有高分结果归因于大模型负例增强。

因此，最准确的实验结论是：

> 大模型生成的纯干扰负例可以训练出有效的独立拒绝头，但其学习到的干扰识别能力没有进一步转化为新闻真假分类性能提升。小权重拒绝可以在不降低 F1 的情况下略微减少评论，大权重或全参数联合训练则会因误删有效证据而产生负面影响。

## 七、最终采用的模型

如果要求最终模型必须实际使用负例，可采用：

```text
checkpoint:
outputs/adaptive_distractor_v7_s42_rdrop100_rejection_only

min_selected = 3
selector_rejection_weight = 0.05
selector_rejection_mode = one_sided_veto
classification threshold = 0.5

Dev Macro-F1 = 0.969119625
Test Macro-F1 = 0.955346651
```

如果论文重点是最高分类性能，则应把 R-Drop、自适应阈值和 `min_selected=3` 作为主要贡献，把大模型负例增强作为消融或失败分析，而不能声称负例增强提高了 F1。

## 八、后续改进方向

如果继续研究负例，不建议只是增加更多同类型的简单纯干扰评论。更有价值的方向是困难负例：

1. 先用当前选择器对候选评论打分；
2. 找出被模型错误赋予高分、但实际没有判别价值的评论；
3. 让大模型生成与新闻主题高度相关、语言自然、但不提供真假证据的评论；
4. 使用人工或自动规则验证其确实为困难负例；
5. 同时提供少量高质量正证据评论，与困难负例形成更可靠的成对监督。

当前 Test 已经被查看，后续新方案不能继续根据该 Test 调参。若开展新实验，应重新建立验证协议并保留未参与参数选择的最终评估数据。

## 九、组会口头汇报稿

本轮我们尝试使用大模型为1000条新闻各生成3条纯干扰评论，共得到3000条负例，希望通过这些负例提升评论选择器的抗干扰能力。数据生成、校验和路由都顺利完成，负例只进入选择器通道，没有污染新闻真假分类证据。

实验中首先发现，最初的高分 R-Drop 模型虽然使用了增强后的训练文件，但负例损失和拒绝权重都设为零，因此当时的提升不能归因于负例。之后我们修正了这个问题，冻结高分主模型，只训练独立拒绝头。参数检查证明只有拒绝头发生变化，说明负例确实参与了训练。

在 Dev 上，拒绝权重设为0.05时，Macro-F1保持0.9691不变，评论保留率略有下降；权重继续增大后，F1逐渐下降。冻结配置在 Test 上的 Macro-F1仍为0.9553，与原模型完全相同。我们还尝试取消冻结进行全参数联合微调，但 Dev Macro-F1下降到0.9588，说明模型变得过度严格并误删了有效评论。

因此，这次实验的结论不是负例模块完全失效，而是模型成功学会了识别人工生成的纯干扰评论，但这种能力没有转化为新闻分类性能提升。后续如果继续做负例增强，需要生成当前选择器容易误选的困难负例，而不是继续增加比较容易识别的普通干扰评论。
