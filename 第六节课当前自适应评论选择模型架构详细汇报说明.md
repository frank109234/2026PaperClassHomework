# 当前自适应评论选择模型架构详细汇报说明

## 一、一句话介绍模型

本模型用于中文新闻真假分类。它不是把一条新闻的全部评论直接平均，而是先结合新闻内容和该新闻的评论集合，为每条新闻预测一个独立的评论选择分界，再从评论中动态选出一个变长的证据子集，最后使用“新闻 + 证据评论”共同判断新闻类别。

当前模型可以概括为：

```text
共享 Chinese BERT 编码器
        +
多尺度 CNN 评论选择表示
        +
每条新闻独立的自适应选择阈值
        +
变长评论证据子集
        +
BERT-CLS 新闻真假分类器
        +
R-Drop 稳定训练
        +
独立的纯干扰负例拒绝头
```

模型是单模型，不使用模型集成，也不使用知识蒸馏。

## 二、研究问题

一条新闻往往对应多条用户评论，但不同评论的价值差异很大：

- 有些评论会补充事实、质疑来源或指出矛盾；
- 有些评论只有情绪表达；
- 有些评论讨论相关话题，但不能帮助判断新闻真假；
- 不同新闻拥有的评论数量和评论质量也不同。

如果把所有评论都保留并等权使用，噪声评论会稀释有效证据。如果统一规定每条新闻保留固定数量的评论，又无法适应新闻之间的差异。

因此，本模型要解决的核心问题是：

> 对于每一条新闻，能否根据新闻语义以及它自己的评论集合，动态决定哪些评论值得进入最终分类？

这里所说的“最佳评论子集”不是人工逐条标注出来的绝对真值，而是指在当前新闻分类目标下，能够帮助模型降低分类损失、同时避免保留过多噪声的任务最优子集。

## 三、整体架构

### 3.1 总体数据流

```text
输入：一条新闻 + 最多32条原始评论
                  │
                  ▼
       共享 Chinese BERT-wwm-ext
                  │
          ┌───────┴────────┐
          │                │
          ▼                ▼
   评论选择表示分支       分类表示分支
 BERT序列+多尺度CNN      BERT [CLS]
   +尺度注意力融合          │
          │                │
          ▼                │
 新闻—评论关系打分          │
          │                │
          ├──────────────┐ │
          │              │ │
          ▼              ▼ │
   新闻级阈值网络      独立负例拒绝头
   预测每条新闻的       只做小幅单向扣分
   独立评论分界              │
          │              │
          └──────┬───────┘
                 ▼
        形成变长硬评论子集
                 │
                 ▼
      对入选评论进行连续加权融合
                 │
                 ▼
          新闻表示 + 证据表示
                 │
                 ▼
            二分类 MLP
                 │
                 ▼
          输出新闻类别概率
```

### 3.2 当前实际启用的主要配置

| 模块 | 当前配置 |
|---|---|
| 预训练编码器 | Chinese BERT-wwm-ext |
| 新闻最大长度 | 256 tokens |
| 评论最大长度 | 96 tokens |
| 每条新闻最多证据评论 | 32 |
| 选择器表示 | `bert_cnn_scale_attn` |
| 分类器表示 | `bert_cls` |
| CNN 卷积核 | 2、3、4 |
| CNN 每尺度通道数 | 128 |
| 选择头 | `pair_mlp` |
| 分类头 | `pair_mlp` |
| 选择模式 | `adaptive_threshold` |
| 训练时选择方式 | `straight_through` |
| 基础训练时最少选择数 | 1 |
| 最终推理时最少选择数 | 3 |
| R-Drop 权重 | 1.0 |
| 稀疏损失权重 | 0.01 |
| 负例拒绝模式 | `one_sided_veto` |
| 最终负例拒绝权重 | 0.05 |

## 四、输入数据如何进入模型

每个样本主要包含：

```text
新闻正文 text
原始评论 comments
新闻类别 label
评论类型 comment_types
评论级标签 comment_labels
```

当前实验明确使用非 clean 数据：

```text
训练：data/weibo21_distractor_only_v7/train.jsonl
Dev： data/weibo21_release_v4/dev.jsonl
Test：data/weibo21_release_v4/test.jsonl
```

其中：

- 训练集有 6120 条新闻；
- 1000 条新闻各加入 3 条大模型生成的纯干扰评论；
- 共有 3000 条纯干扰负例；
- 不使用立场负例；
- 不使用去掉强标签的 clean 版本。

### 4.1 原评论和生成负例走不同通道

数据加载时会先识别：

```text
comment_label = 0
comment_type = pure_distractor
```

满足条件的生成评论会从真假分类证据中移除，只进入 `selector_negative` 通道。

因此模型内部存在两个相互隔离的评论集合：

```text
原始评论
  └─ 可以进入自适应选择器和新闻分类器

生成纯干扰评论
  └─ 只用于训练独立拒绝头，不进入新闻真假分类证据
```

这个设计可以避免大模型生成文本直接污染新闻分类标签。

## 五、共享 Chinese BERT 编码器

新闻和评论都由同一个 Chinese BERT-wwm-ext 编码。共享参数有两个好处：

1. 新闻和评论进入同一个语义空间，便于比较它们的关系；
2. 不需要分别训练两个完整 BERT，减少参数冗余。

对于每段文本，BERT 输出一列上下文 token 表示：

```text
[CLS], token_1, token_2, ..., token_L
```

同一组 BERT 输出会被后续两个任务分支以不同方式读取：

- 选择器需要更细粒度的局部短语特征；
- 分类器更偏向使用整体语义表示。

因此模型不是简单地让选择器和分类器完全共用同一种文本表示，而是在共享 BERT 的基础上使用两个不同的表示头。

## 六、评论选择表示：BERT + 多尺度 CNN + 尺度注意力

当前选择器使用 `bert_cnn_scale_attn`。它的目标是同时理解：

- 整段评论的大意；
- 评论中的局部关键词和短语；
- 对当前文本而言，哪一种短语尺度更重要。

### 6.1 BERT 的整体语义

BERT 的 `[CLS]` 表示可以看作整段文本的语义摘要：

```text
r_cls = BERT(text)[CLS]
```

它擅长表达整体语境，但一些局部模式可能被压缩。例如：

```text
“没有官方通报”
“来源在哪里”
“已经辟谣”
```

这些短语对评论是否具有证据价值可能很重要。

### 6.2 多尺度 CNN 提取局部模式

模型在 BERT 的上下文 token 序列上使用三组一维卷积：

```text
卷积核 2：关注相邻两个 token 的模式
卷积核 3：关注三个 token 的短语
卷积核 4：关注更长的局部表达
```

每个尺度经过 GELU 激活和最大池化，得到该尺度最明显的局部特征。

通俗地说，BERT 负责“读懂整句话”，CNN 负责“找出最值得注意的短语”。

### 6.3 尺度注意力

不同评论的重要短语长度不一样，因此不能固定认为某个卷积核最好。

模型利用 `[CLS]` 与三个尺度的平均摘要生成查询，再对 2、3、4 三个尺度计算注意力权重：

```text
a_2 + a_3 + a_4 = 1
```

然后得到加权 CNN 表示：

```text
r_cnn = a_2 r_2 + a_3 r_3 + a_4 r_4
```

### 6.4 门控残差融合

最后，模型不是直接用 CNN 替换 BERT `[CLS]`，而是学习一个门控：

```text
g = sigmoid(MLP([r_cls; r_cnn]))
r_selector = LayerNorm(r_cls + g * r_cnn)
```

如果某条文本主要依赖整体语义，门控可以少加入 CNN 信息；如果局部短语很重要，门控可以增加 CNN 的贡献。

这一步得到新闻和每条评论的选择表示。

## 七、新闻—评论关系打分

得到新闻选择表示 `n` 和评论选择表示 `c_i` 后，模型构造四类关系特征：

```text
z_i = [n;
       c_i;
       |n-c_i|;
       n*c_i]
```

四部分可以通俗理解为：

| 特征 | 含义 |
|---|---|
| `n` | 新闻本身在说什么 |
| `c_i` | 评论本身在说什么 |
| `|n-c_i|` | 新闻和评论有哪些差异 |
| `n*c_i` | 新闻和评论有哪些语义匹配 |

四路特征进入两层 MLP：

```text
4H → H → 1
```

输出评论关系分数 `s_i`。分数越高，表示该评论越值得作为当前新闻的证据候选。

这里的分数是“新闻—评论对”分数。同一条评论放在不同新闻下，得到的分数可以不同。

## 八、新闻级自适应阈值

### 8.1 为什么不再使用固定 0.2

旧方案虽然为每条评论输出不同分数，但所有新闻最终都使用同一个固定阈值，例如 0.2。

这会产生问题：

- 有些新闻的全部评论质量都比较高；
- 有些新闻评论很多但噪声很大；
- 不同模型和不同随机种子的分数尺度可能不同；
- 一个全局常数无法同时适配所有新闻。

### 8.2 每条新闻预测自己的分界

当前模型先计算该新闻全部有效评论的平均表示：

```text
c_bar = 有效评论表示的平均值
```

再结合：

```text
新闻表示 n
评论集合摘要 c_bar
二者绝对差 |n-c_bar|
二者逐元素乘积 n*c_bar
有效评论数量比例
```

输入一个阈值 MLP，得到该新闻独有的阈值 logit `b`：

```text
b = base_logit + threshold_MLP(context)
```

模型初始化时从概率 0.5 对应的位置开始，但最后并不是所有新闻都使用 0.5。阈值网络会根据新闻和评论集合学习不同偏移。

### 8.3 评论是否入选

每条评论的连续保留概率为：

```text
q_i = sigmoid((s_i-b)/T)
```

推理时采用硬判断：

```text
s_i >= b  → 评论自然入选
s_i <  b  → 评论自然不入选
```

因此，模型不使用固定 top-k，也不使用全局固定阈值。不同新闻可以选择不同数量的评论。

### 8.4 平均阈值不是统一阈值

最终模型在 Test 上报告：

```text
mean adaptive threshold = 0.570469
adaptive threshold std  = 0.037735
```

这里的 0.570469 只是 1748 条 Test 新闻各自阈值的平均值。标准差明显非零，说明不同新闻实际使用的阈值不同，并没有退化成新的固定阈值。

## 九、最少评论约束 `min_selected`

自适应阈值可能让某些新闻没有任何评论自然入选。为了避免分类器完全失去评论证据，模型加入最少选择约束。

最终推理采用：

```text
min_selected = 3
```

规则是：

```text
自然入选评论数 >= 3
    → 保持自适应阈值结果

自然入选评论数 < 3
    → 从当前新闻评论中补选得分最高的评论，直到3条

新闻不足3条评论
    → 保留全部现有评论
```

它不是固定 top-3。模型仍然可以自然选择 4 条、8 条或更多评论；`3` 只是防止证据集合过小的下限。

基础模型训练时使用 `min_selected=1`，随后仅在 Dev 上比较推理下限，seed 42 在 `min_selected=3` 时取得最佳 Dev 结果，因此最终冻结为 3。

## 十、直通估计：训练时如何学习硬子集

硬判断 `s_i >= b` 是离散操作，普通反向传播无法直接穿过这个判断。

如果训练时使用所有评论、推理时才突然改成硬子集，又会出现训练和推理不一致。

因此模型使用 `straight_through`：

```text
前向传播：真正使用硬选择子集做分类
反向传播：使用连续概率 q_i 近似硬掩码的梯度
```

可以把它理解为：

> 模型做决定时按照“选或不选”执行，但学习时仍保留一条平滑的梯度通道，知道怎样调整评论分数和新闻阈值。

这样，新闻分类损失可以反向更新：

- BERT 编码器；
- 多尺度 CNN；
- 尺度注意力；
- 评论关系打分头；
- 新闻级阈值网络；
- 最终分类器。

## 十一、证据评论如何融合

评论入选以后，模型不是简单做等权平均。

对于入选评论，继续使用其连续证据门控分数作为权重，再归一化：

```text
w_i = q_i / sum(q_j),  i属于入选子集
```

得到新闻级证据表示：

```text
e = sum(w_i * c_i_classifier)
```

这里用于融合的评论向量来自分类表示分支，而不是选择器的 CNN 表示。

这种设计把两个问题分开：

```text
选择器分支：哪些评论应该进入证据集合？
分类器分支：入选评论表达了什么分类信息？
```

负例拒绝头只改变评论是否进入子集，不直接改变已经入选评论的主证据权重。

## 十二、分类表示和最终分类器

当前分类器使用 BERT `[CLS]` 表示：

```text
n_cls = 新闻的 BERT [CLS]
c_i_cls = 评论的 BERT [CLS]
```

入选评论融合为证据向量 `e` 后，再构造：

```text
z_cls = [n_cls;
         e;
         |n_cls-e|;
         n_cls*e]
```

最后进入 `pair_mlp`：

```text
4H → H → 2
```

输出两个类别的 logits，再通过 softmax 得到新闻分类概率。

选择器和分类器都使用四路关系特征，但作用不同：

- 选择器预测每条评论是否值得保留；
- 分类器预测整条新闻的类别。

## 十三、R-Drop 如何提高稳定性

R-Drop 只在训练阶段使用。

同一个 batch 在 dropout 开启时做两次前向传播：

```text
同一条新闻和同一组评论
        ├─ 第一次 dropout → logits_A
        └─ 第二次 dropout → logits_B
```

训练目标要求：

1. 两次输出都能正确分类；
2. 两次输出的概率分布不要相差太大。

R-Drop 损失为对称 KL 散度：

```text
L_rdrop = 1/2 [KL(P_A || P_B) + KL(P_B || P_A)]
```

训练总损失中的主干部分为：

```text
L_main = 1/2(L_cls_A + L_cls_B)
       + lambda_rdrop * L_rdrop
       + lambda_sparse * L_sparse
```

当前配置：

```text
lambda_rdrop = 1.0
lambda_sparse = 0.01
```

R-Drop 可以理解为要求模型“即使内部随机丢弃了一部分神经元，也要给出相近判断”，从而减少训练偶然性和过拟合。

推理阶段关闭 dropout，只进行一次前向传播，因此不会使最终推理翻倍。

## 十四、稀疏损失的作用

如果只优化分类损失，选择器可能倾向于保留大量评论，因为多保留评论没有额外代价。

模型加入轻量稀疏成本：

```text
L_sparse = 所有有效评论保留概率的平均值
```

它不是要求所有新闻保留相同比例，而是给“保留一条评论”增加轻微成本。

只有当一条评论对新闻分类足够有价值时，分类收益才值得抵消这项成本。

当前权重为 0.01，目的是避免选择器退化成全选，而不是追求评论越少越好。

## 十五、纯干扰负例拒绝头

### 15.1 为什么单独设置拒绝头

3000 条纯干扰负例由大模型生成。如果直接让它们更新主选择器和 BERT，模型可能学习生成文本风格，并干扰已经收敛的新闻分类能力。

因此模型复制主选择头，形成独立拒绝头：

```text
主选择头
  └─ 由新闻分类损失和稀疏损失学习证据价值

独立拒绝头
  └─ 由原评论—纯干扰负例排序损失学习干扰特征
```

### 15.2 负例排序目标

对于包含生成负例的新闻，拒绝头希望至少有一条原评论的分数高于生成负例，并保留一定 margin：

```text
L_neg = ReLU(margin + score_negative - max(score_original))
```

当前 margin 为 1.0。

`pairwise_bias_cancel` 使用分数差训练，避免模型只通过整体下调输出偏置来完成任务。

### 15.3 两阶段训练

最终负例模型不是把所有模块一起重训，而是：

```text
阶段一：训练 R-Drop 自适应选择主模型
        selector_loss_weight = 0

阶段二：载入阶段一 checkpoint
        冻结 BERT、主选择器、阈值网络和分类器
        只训练独立拒绝头
        selector_loss_weight = 0.1
```

参数检查证明：

```text
拒绝头参数发生变化
主模型非拒绝头参数变化数 = 0
```

### 15.4 单向 veto

拒绝头不允许提高任何评论的主选择分数，只能对可疑评论做非正扣分：

```text
v_i = min(centered_rejection_score_i, 0)
s_i_final = s_i_main + alpha * v_i
```

当前冻结权重：

```text
alpha = 0.05
```

因为 `v_i <= 0`，拒绝头只能轻微否决，不能把原本低分的评论抬高。

Dev 实验表明，更大的权重会误删有效原评论，因此只采用 0.05。

### 15.5 负例模块的真实贡献

负例模块在 Test 上没有提高 F1，但略微降低了评论保留率：

| 设置 | Test Macro-F1 | 评论保留率 |
|---|---:|---:|
| 不启用拒绝头 | 0.955347 | 0.149504 |
| 启用负例拒绝头，权重 0.05 | 0.955347 | 0.149343 |

因此不能声称大模型负例增强提升了分类性能。准确结论是：负例拒绝头确实参与了筛选，并在不改变分类结果的情况下过滤了极少量评论。

## 十六、当前数据没有激活去情绪化双视图

代码支持原评论视图和经过验证的去情绪化评论视图，并可对两个视图的分类损失取平均。

但当前实验按照要求使用非 clean 的 `weibo21_distractor_only_v7` 和 `weibo21_release_v4`，当前训练路由审计中有效去情绪化评论数为 0。

因此，本次最终模型实际执行的是：

```text
新闻原文 + 原始评论单视图
```

而不是去情绪化双视图。汇报时应区分：

- 代码保留双视图兼容能力；
- 当前实验没有启用该能力；
- 当前结果不能归因于去情绪化数据。

## 十七、训练阶段与推理阶段的完整流程

### 17.1 主模型训练

```text
新闻和原评论
    ↓
共享 BERT 编码
    ↓
选择器分支生成评论分数
    ↓
阈值网络生成每条新闻独立分界
    ↓
straight-through 形成硬子集
    ↓
入选评论加权融合
    ↓
新闻真假分类
    ↓
分类损失 + R-Drop + 稀疏损失
    ↓
更新 BERT、选择器、阈值网络和分类器
```

### 17.2 负例拒绝头训练

```text
载入高分主模型
    ↓
冻结所有主模型参数
    ↓
原评论与3000条生成负例进入拒绝头
    ↓
计算成对 margin 排序损失
    ↓
只更新独立拒绝头
```

### 17.3 最终推理

```text
新闻和最多32条原评论
    ↓
主选择头输出评论分数
    ↓
拒绝头以0.05权重做单向扣分
    ↓
与该新闻自己的动态阈值比较
    ↓
不足3条时补选最高分评论
    ↓
形成变长证据子集
    ↓
按主证据概率加权融合
    ↓
输出新闻类别
```

## 十八、关键损失函数总结

主模型阶段：

```text
L_stage1 = L_classification
         + 1.0 * L_RDrop
         + 0.01 * L_sparse
```

独立拒绝头阶段：

```text
L_stage2 = 0.1 * L_negative_ranking
```

最终推理没有损失函数，只执行：

```text
主选择分数
+ 0.05 * 单向拒绝扣分
与新闻级动态阈值比较
```

## 十九、最终单模型结果

最终实际使用负例的单模型：

```text
outputs/adaptive_distractor_v7_s42_rdrop100_rejection_only
```

冻结推理配置：

```text
min_selected = 3
selector_rejection_weight = 0.05
selector_rejection_mode = one_sided_veto
classification threshold = 0.5
```

结果：

| 数据集 | Macro-F1 | Accuracy | 评论保留率 | 平均动态阈值 |
|---|---:|---:|---:|---:|
| Dev | 0.969120 | 0.969143 | 0.141210 | 0.571203 |
| Test | 0.955347 | 0.955378 | 0.149343 | 0.570469 |

Test 阈值标准差为 0.037735，说明新闻之间确实使用不同分界。

## 二十、随机种子稳定性

R-Drop 主架构在三个随机种子、统一 `min_selected=3` 下的 Dev Macro-F1：

| Seed | Dev Macro-F1 |
|---:|---:|
| 42 | 0.969120 |
| 43 | 0.953072 |
| 44 | 0.963403 |
| 均值 | 0.961865 |
| 总体标准差 | 0.00664 |

三个 seed 的 Test Macro-F1 均值为：

```text
0.953023 ± 0.00288
```

这说明模型总体有效，但仍存在随机种子波动。seed 42 是根据 Dev 选择的最终单模型，不能只报告 seed 42 而完全忽略多 seed 稳定性结果。

负例拒绝头的完整两阶段实验目前主要在 seed 42 上完成，因此负例模块的跨 seed 稳定性不能被过度表述。

## 二十一、取消冻结为什么失败

在只训练拒绝头之后，还尝试取消冻结并对全部参数联合微调一轮。

结果：

```text
冻结主模型 Dev Macro-F1：0.969120
全部取消冻结 Dev Macro-F1：0.958842
```

同时平均动态阈值从 0.571203 升到 0.590702，说明模型变得过度严格。

原因是已经收敛的 BERT、主选择器和分类器继续适应新的筛选子集，产生过拟合和阈值漂移。评论数量减少，但有效证据也被误删。

因此最终保留两阶段冻结方案，联合微调模型只作为失败消融。

## 二十二、模型的主要创新点与边界

### 22.1 可以作为主要贡献的部分

1. **新闻级自适应阈值**：每条新闻根据自身语义和评论集合预测独立分界，不再共享固定 0.2；
2. **多尺度评论选择表示**：结合 BERT 全局语义、2/3/4-gram CNN 和尺度注意力；
3. **硬子集前向、连续梯度反向**：缩小训练和推理之间的差异；
4. **变长证据集合**：不固定 top-k，只设置最低证据数量；
5. **R-Drop 稳定训练**：约束两次 dropout 前向输出一致；
6. **选择和分类表示解耦**：选择器关注局部证据模式，分类器使用 BERT-CLS 完成最终判断。

### 22.2 只能作为辅助或消融的部分

1. 大模型纯干扰负例没有提升 F1；
2. 负例拒绝头只带来极小的评论保留率下降；
3. 取消冻结联合训练会明显退化；
4. 当前数据没有启用去情绪化双视图；
5. “最佳评论子集”没有人工评论级金标准，是通过新闻分类目标间接学习的任务最优子集。

## 二十三、老师可能追问的问题

### 问题1：平均阈值约为0.57，这是不是又变成固定阈值？

不是。0.57 是所有新闻独立阈值的平均值。Test 阈值标准差约为0.0377，不同新闻的阈值由其新闻语义、评论集合摘要和评论数量共同决定。

### 问题2：`min_selected=3` 是否等同于 top-3？

不等同。top-3 强制每条新闻只选3条；本模型允许自然选择任意数量，只在不足3条时补到3条。

### 问题3：没有评论级最佳证据标签，选择器怎么训练？

主选择器通过新闻分类损失间接学习。如果保留某条评论能降低分类损失，该评论会获得正向梯度；稀疏损失同时抑制无代价的全选。它学习的是任务驱动证据，而不是人工定义的评论质量。

### 问题4：为什么选择器需要 CNN，分类器却只用 BERT-CLS？

选择器需要识别评论中的局部质疑、否定、来源和辟谣短语，因此使用多尺度 CNN；最终分类更依赖新闻和融合证据的整体语义，实验中 BERT-CLS 更稳定。

### 问题5：负例既然没有提升 F1，是否说明生成失败？

不是。生成、路由和拒绝头训练均成功。失败的是“简单纯干扰负例能够提升新闻分类”这一性能假设。负例过于容易，并且与真实评论分布存在差异。

### 问题6：为什么不直接让负例进入分类器？

生成负例没有新闻真假标签意义。把它们作为新闻证据可能让分类器学习大模型文本风格并污染主任务，因此只用于评论拒绝训练。

### 问题7：为什么最后冻结主模型？

冻结实验能够保证负例只训练拒绝头，不破坏已经收敛的主模型。取消冻结后 Dev F1 从0.9691降到0.9588，实验证明冻结是必要的。

### 问题8：当前结果是否使用 clean 数据？

没有。训练使用 `weibo21_distractor_only_v7`，Dev/Test 使用 `weibo21_release_v4`，均按当前非 clean 实验协议执行。

### 问题9：当前是否使用模型集成或知识蒸馏？

没有。最终结果来自一个 checkpoint 的单模型推理。评估脚本中的 `ensemble` 字段只是通用输出格式，传入一个 checkpoint 时不构成集成。

### 问题10：Test F1 为什么低于 Dev？

Dev 为0.9691，Test为0.9553，下降约1.38个百分点。两个数据集上的平均动态阈值和阈值标准差非常接近，说明下降不像是阈值模块失效，更可能是数据分布差异和样本随机性。

## 二十四、建议的汇报顺序

汇报时建议按以下顺序，不要一开始陷入公式：

1. 先说明评论中有有效证据也有噪声；
2. 解释旧固定阈值无法适应不同新闻；
3. 展示整体架构图；
4. 重点讲 BERT-CNN 选择表示和新闻级自适应阈值；
5. 解释 `min_selected=3` 只是下限，不是固定 top-3；
6. 说明 R-Drop 如何稳定训练；
7. 再把大模型负例作为辅助实验讲解；
8. 最后如实说明负例没有提升 F1，但验证了过滤与冻结策略；
9. 用 Dev/Test 结果和局限性收尾。

## 二十五、可直接照着讲的架构汇报稿

我们的任务是结合新闻正文和用户评论进行真假分类。评论中既有有效证据，也有大量情绪表达和无关内容，因此核心问题不是简单加入评论，而是针对每条新闻动态选择有价值的评论子集。

模型首先使用共享的 Chinese BERT-wwm-ext 编码新闻和评论。在评论选择分支中，我们在 BERT 上下文序列后加入2、3、4三种卷积核，提取不同长度的局部短语特征，再通过尺度注意力判断当前文本更依赖哪一种短语尺度。CNN 特征通过门控残差与 BERT 的整体语义融合，得到专门用于评论选择的表示。

对于每一个新闻—评论对，我们拼接新闻表示、评论表示、绝对差和逐元素乘积，通过 MLP 得到评论关系分数。与过去所有新闻共享固定0.2阈值不同，当前模型还会汇总该新闻的评论集合，并结合新闻语义和评论数量，为这条新闻单独预测一个选择阈值。评论分数超过当前新闻自己的阈值时才自然入选，因此不同新闻可以保留不同数量的评论。

训练时采用直通估计：前向传播真正使用硬评论子集进行分类，反向传播则使用连续门控近似梯度，使分类损失能够更新评论打分头和阈值网络。最终推理设置最少保留3条评论，但这不是固定top-3；如果模型自然选择了更多评论，就会全部保留。

入选评论不会简单等权平均，而是继续按照主选择器的连续概率加权融合。分类分支使用 BERT-CLS 表示，将新闻表示、融合证据表示、绝对差和逐元素乘积送入二分类 MLP，得到新闻类别。

为了提高训练稳定性，我们使用 R-Drop，对同一输入进行两次带 dropout 的前向传播，并约束两次预测分布一致。R-Drop、自适应阈值和最少评论约束构成当前性能提升的主要来源。

此外，我们为1000条新闻各生成3条纯干扰评论，共3000条负例。负例不进入新闻分类证据，只用于训练独立拒绝头。最终采用0.05的小权重单向否决，它可以略微减少评论保留量，但没有提升 Test F1。更大的拒绝权重和取消冻结联合训练都会误删有效评论，因此负例模块只能作为辅助消融，不能作为主要性能贡献。

最终单模型在 Dev 上 Macro-F1 为0.9691，在 Test 上为0.9553。Test 中平均自适应阈值约为0.5705，标准差约为0.0377，说明模型确实学习到了新闻之间不同的选择分界，而不是用新的统一常数替代旧的0.2阈值。
