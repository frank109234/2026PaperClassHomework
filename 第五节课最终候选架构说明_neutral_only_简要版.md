# 方法架构说明（简要版）

## 1. 总体架构

当前模型主要由两个模块组成：

1. **评论选择器**：判断每条评论对当前新闻的价值，并生成动态评论子集；
2. **真假分类器**：聚合选择后的评论证据，结合新闻内容完成真假分类。

两个模块共享同一个 Chinese BERT-wwm-ext 编码器。去情绪化双视图属于训练方式，不是第三个网络模块。

总体流程如下：

```text
新闻正文 + 原始评论
        ↓
共享 Chinese BERT 编码
        ↓
评论选择器计算每条评论的保留分数
        ↓
训练时连续加权 / 推理时阈值筛选
        ↓
聚合保留评论，形成评论证据向量
        ↓
真假分类器建模新闻与评论证据的关系
        ↓
输出真实或虚假
```

## 2. 评论选择器

评论选择器负责回答：**对于当前新闻，哪些评论适合作为分类证据？**

### 2.1 BERT 上下文编码

新闻和评论分别输入共享 BERT，得到 token 级上下文序列和 `[CLS]` 全局表示：

```text
新闻   → BERT → 新闻上下文序列、新闻 CLS
评论 i → BERT → 评论上下文序列、评论 CLS
```

BERT 隐藏维度为 768。新闻最大长度为 256，单条评论最大长度为 96，每条新闻最多使用 32 条证据评论。

### 2.2 多尺度 CNN

选择器在 BERT 上下文序列上使用卷积核为 2、3、4 的一维 CNN：

```text
BERT 上下文序列
    ├─ 2-token 卷积
    ├─ 3-token 卷积
    └─ 4-token 卷积
```

每个卷积分支包含 128 个通道。卷积特征经过 GELU 和最大池化，形成三个不同尺度的局部特征。

### 2.3 尺度注意力

三个 CNN 特征不会直接拼接。模型先将它们分别投影到 768 维，再根据当前文本的 `[CLS]` 生成查询，对三个尺度计算注意力权重：

```text
2-token 特征 ─┐
3-token 特征 ─┼─ 尺度注意力 → 局部融合特征
4-token 特征 ─┘
```

因此，不同新闻或评论可以为 2、3、4 三种局部尺度分配不同权重。

### 2.4 门控残差融合

尺度注意力得到的局部特征与 BERT `[CLS]` 进行门控残差融合：

```text
选择表示 = LayerNorm(CLS + 门控值 × 局部特征)
```

最终分别得到新闻选择表示和评论选择表示。

### 2.5 新闻—评论关系打分

对新闻和每条评论构造四路关系特征：

```text
新闻表示
评论表示
|新闻表示 - 评论表示|
新闻表示 × 评论表示
```

四部分拼接后输入 `pair_mlp`：

```text
4 × 768 → 768 → 1
```

经过 sigmoid 后得到每条评论的连续保留分数。

### 2.6 训练与推理的选择方式

训练阶段不直接删除评论，而是让所有有效评论按照连续分数参与证据聚合。这样，真假分类损失可以反向更新评论选择器。

验证和推理阶段使用固定阈值 0.20：

```text
保留分数 ≥ 0.20 → 保留
保留分数 < 0.20 → 删除
```

如果一条新闻存在评论，但所有评论都低于阈值，则补选分数最高的一条。选择数量由评论分数决定，不是固定 top-k。

## 3. 真假分类器

真假分类器负责回答：**新闻内容与筛选后的评论证据组合起来，更支持真实还是虚假？**

### 3.1 评论证据聚合

分类分支直接使用每条评论的 BERT `[CLS]`。被选择的评论按照选择器分数归一化加权：

```text
评论证据 = Σ（评论权重 × 评论 CLS）
```

由此将数量可变的评论集合压缩为一个 768 维评论证据向量。

训练时，权重来自所有有效评论的连续分数；验证和推理时，未进入硬子集的评论权重严格为 0。

### 3.2 新闻—证据关系分类

分类器使用新闻 BERT `[CLS]` 和评论证据向量构造四路关系：

```text
新闻 CLS
评论证据
|新闻 CLS - 评论证据|
新闻 CLS × 评论证据
```

拼接后输入分类 `pair_mlp`：

```text
4 × 768 → 768 → 2
```

最后输出真实和虚假两个类别的 logits。

## 4. 去情绪化双视图训练

部分训练评论具有经过验证的去情绪化版本。对于这些样本，模型构造两个评论视图：

```text
原评论视图
去情绪化评论视图
```

评论选择器只根据原评论计算一次评论权重。去情绪化视图不重新运行选择器，而是复用原视图的同一组权重：

```text
原评论 + 原选择权重 → 原评论证据
去情绪化评论 + 同一选择权重 → 去情绪化评论证据
```

两个证据向量经过同一个真假分类器。对于存在有效去情绪化评论的样本，分类损失为：

```text
样本损失 = 0.5 × 原评论分类损失
         + 0.5 × 去情绪化评论分类损失
```

没有有效去情绪化评论的样本只计算原评论分类损失。

最终模型没有额外启用语义余弦一致性损失、预测分布 KL 损失或评论稀疏损失。去情绪化增强通过两套视图的真假分类交叉熵参与训练。

## 5. 最终训练目标

最终损失可以概括为：

```text
没有去情绪化视图：
    L = CE（原评论视图预测，新闻标签）

存在去情绪化视图：
    L = 0.5 × CE（原评论视图预测，新闻标签）
      + 0.5 × CE（去情绪化视图预测，新闻标签）
```

虽然没有评论级正负标签监督，分类损失仍会通过评论证据权重反向传播到选择器，使选择器学习哪些评论组合更有利于真假分类。

## 6. 最终训练和推理流程

### 6.1 训练流程

```text
1. 输入新闻、原评论和可用的去情绪化评论
2. 共享 BERT 编码新闻和评论
3. 选择器为原评论计算连续保留分数
4. 使用全部有效评论的连续分数聚合原评论证据
5. 对有去情绪化视图的样本，复用同一权重聚合去情绪化证据
6. 分类器分别输出原视图和去情绪化视图预测
7. 计算单视图或双视图分类损失
8. 端到端更新 BERT、CNN、尺度注意力、选择头和分类头
```

### 6.2 推理流程

```text
1. 输入新闻和原始评论
2. 共享 BERT 编码新闻和评论
3. 选择器计算每条评论的保留分数
4. 使用阈值 0.20 形成动态评论子集
5. 在子集内按保留分数加权聚合评论证据
6. 分类器结合新闻和评论证据输出真假预测
```

推理阶段不需要去情绪化评论，也不需要调用大语言模型。

## 7. 当前主模型边界

训练文件中虽然包含 494 条纯干扰和纯立场生成评论，但最终主模型设置 `max_selector_negatives=0`、`selector_loss_weight=0`，因此这些评论：

- 不进入真假分类证据；
- 不参与最终评论选择器训练；
- 只保留为负例排序和拒绝头的消融实验数据。

所以当前主模型应描述为：

> 多尺度动态评论选择器与 BERT 证据分类器组成两阶段网络，并使用去情绪化双视图分类进行训练。

